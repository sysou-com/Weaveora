# 可行性分析 · 对口型「大喊/惊吼」镜的嘴型方案（2026-09）

> 结论性文档（按仓库纪律：评审/战略结论只写 `可行性分析*.md`，不落 `Weaveora.md`）。
> 触发：用户反馈「对口型里关于大喊、惊吼等场景，嘴型处理方案和效果不可接受」。
> 状态：**方案已评审待批，未执行**。

---

## 0. 结论速览

1. **不是调参问题，是工具选型错配**：现在的对口型（LatentSync / MuseTalk / Wav2Lip / KeySync）都是**嘴部掩码替换**——
   只重画嘴周围那一块，**下颌与下巴的开合不在模型职责内**。大喊镜必然两头不讨好：
   - 用大张口底片 → 掩码要和"已经掉下来的下颌"对齐 → 几何冲突 → **画面被破坏**；
   - 用闭嘴底片 → 稳定但**开合不足 → 不像在喊**。
2. **三条正路（按性价比）**：① 导演层规避（0 成本、最稳）② 云端整脸/原生音频生成（Veo 3.1 / WAN-S2V，按镜付费）
   ③ 本地音频驱动视频生成（WAN2.2-S2V / InfiniteTalk(v2v 保运镜) / EchoMimicV3(1.3B 最省)）。
3. **附加保底**：LatentSync 通道 + 自适应嘴部 mask + 更高工作分辨率 + 出口包络 QC（能减少破坏，但**做不出呐喊的大开合**）。
4. **中期正解**：**jaw-lip 解耦**（音频分别驱动"下颌开合"与"嘴唇形状"）——这是 JALI 的原始设计思想（JALI = **J**aw **A**nd **L**ip），
   也是唯一能同时做到「喊得开 + 画面干净 + 美术可控」的路。详见 §4。
5. **GPU 服务器加存储：可行且便宜，但不是瓶颈**。模型体积是几十 GB 级；**真正的瓶颈是 24GB 单卡显存与串行执行**。
   见 §5（当前数据盘 100 GB，已用 ~50 GB；本方案新增约 73 GB → 建议数据盘 ≥200 GB）。

---

## 1. 问题与实测证据（同项目真实素材）

同项目实测 `mouth_open`（人脸服务，106 点：嘴部中央区上下唇间距 ÷ 嘴宽）：

| 素材 | 分辨率 | face_px | mouth_open | 说明 |
|---|---|---|---|---|
| 平静脸（第1镜） | 2560×1440 | 287 | 0.53 | 低语温存 |
| 平静脸（第4镜） | 2560×1440 | 392 | 0.66 | 对白 |
| 定妆照 | — | 296 | 0.44 | 中性表情 |
| 第6镜底片（喊叫但近景+被安抚） | — | 220/216 | 0.59 / 0.78 | 正常区间，B 不拦 |
| **第5镜底片（真·大张口）** | 2560×1440 / 1280×704 | 284 / 96 | **1.232 / 1.238** | 就是「画面被破坏」那一镜 |
| 第5镜新「闭嘴近景」底片 | 2560×1440 | 747 | **0.409** | 重出后当底片 |
| **第5镜新对口型产物** | 1280×720 | 377 | **0.443** | 稳定，但落在"正常说话"区间 → **不像在喊** |

两个失败模式（同一个结构原因）：

- **几何冲突**：底片下颌已掉下来，掩码内的"新嘴"与掩码外的"旧下颌"对不上 → 形变/缝合/双嘴。
- **开合不足**：模型只会依据音频在其先验范围内开合，**下颌位移不在它的输出空间里** → 喊叫看起来像"轻描淡写地说话"。

旁证（外部资料）：

- NVIDIA Audio2Face 社区共同结论：要得到"张着下巴 + 嘴唇闭合/整形"的正确形状，必须把 **`JawOpen` 与 `MouthClose` 当作两个独立通道**联合使用。
- JALI（SIGGRAPH 2016）与 VisemeNet：**shouting 这类说话风格是"整脸运动"**（下颌+颊+眉），不是音素/viseme 问题。
- 2026 年开源横评：LatentSync 1.6 仍是"本地重配音基线"，KeySync 是最接近的直接替代；InfiniteTalk 属于"更重的 talking-video 系统，不是简单换嘴"。
- 另一类观察：AI 配音"看起来假"很多时候是**合成缝合问题**（掩码边界色差/抖动），不是嘴本身——所以"换整脸生成"和"修 mask"要分开评估。

---

## 2. 候选方案全景（2026-09 口径）

| 方案 | 类型 | 对大喊镜适配 | 资源/价格 | 接入 | 难度 |
|---|---|---|---|---|---|
| **LatentSync 1.6**（现状） | 嘴部替换 | ❌ 结构受限 | 24G 舒适 | 已有 ComfyUI 工作流 | — |
| KeySync（+Advanced/SAM2） | 嘴部替换+遮挡 | ⚠️ 仍补嘴，可处理手/爪挡脸 | 24G | ComfyUI 节点 | 低 |
| MuseTalk 1.5 | 嘴部替换 | ⚠️ 求快/预览档 | 轻 | ComfyUI | 低 |
| **Wan2.2-S2V-14B** | **音频驱动整脸**（图+音频+提示词→视频） | ✅ 下颌一起生成 | 本地 fp8 **16.4 GB** 权重（+ umt5 6.27G 复用 + wav2vec2 + VAE）；云端 Replicate `wan-video/wan-2.2-s2v` | ComfyUI **官方模板** / Replicate API | 中 |
| **InfiniteTalk** | **视频重配音(v2v)**+i2v，保留原运镜/背景，整脸+身体对齐 | ✅ 运镜已定稿时最优 | Wan2.1-I2V-14B fp8 + InfiniteTalk 权重 ≈ **20 GB**（基础）；Wan2GP 低显存版 | ComfyUI 分支 | 中 |
| **EchoMimicV3**（AAAI'26，1.3B） | 音频驱动（**可编辑 landmark 条件**） | ✅ 便宜档 + 下颌可控 | 权重 **7.14 GB**（含 base 1.3B + wav2vec2）；**12G / 16G 可跑** | 社区 ComfyUI 节点 | 低 |
| MultiTalk | 多人对话音频驱动 | ✅ 多人同框 | 14B 级 | ComfyUI | 中 |
| HunyuanVideo-Avatar | 音频驱动+情绪对齐+多角色 | ✅ 强但重 | 13B 级（+15~26 GB） | 官方/社区 | 高 |
| MEMO / OmniAvatar | 表达力/半身 | ✅ 研究向 | 14B 级 | 官方代码 | 高 |
| **Veo 3.1**（Replicate / FAL） | **原生同步音频**（一次生成带对白） | ✅✅ 喊叫镜"一把过" | ≈**$0.10/s**（8s 原生） | 已有 Replicate 通道 | **低** |
| **sync.so**（lipsync-2 / -pro / sync-3） | 云端对口型（**内置遮挡检测**、4K） | ⚠️ 仍补嘴，但"最自然"+遮挡 | **$0.04–0.133/s** | ComfyUI 内置 `SyncLipSyncNode` 或直连 | 低 |
| LTX 2.3 LipDub | 新 dubbing IC-LoRA | ⚠️ 看栈 | 重（22B） | LTX 栈 | 中 |
| ~~Sora 2~~ | 原生音频 | — | **不要押**（公开报道其 API 2026-09-24 关停） | — | — |

---

## 3. 推荐路线（分阶段）

### P0 · 当天可做，0 成本（最稳的一招）
1. **导演层硬规则**：台词含 喊叫/尖叫/失声/吼/嚎 的镜 → **禁止正脸中近景**；只允许 画外音 / 背影 / 侧脸 / 剪影 / 手与道具 / 环境反应镜头，或「≤2 帧张口极限帧 + 快切」。
   在 `DirectorPlanValidator` 里做成硬校验（违反即报错，别等生成完才发现）。
2. **QC 从"静态阈值"升级为"包络匹配"**：由音频算期望张口曲线，再量输出每帧张口度 →
   验收 = `峰值比 + 相关性 + 下颌区时域差分(缝合/闪烁) + ArcFace 身份相似`。

### P1 · 1–3 天，先云验证（$1–3 量级）
3. 喊叫/惊吼镜改走**整脸生成**：优先 **Veo 3.1（原生音频）**；要保留自研画面形象则用 **Replicate `wan-video/wan-2.2-s2v`**。
   两条都复用现有 Replicate 通道与 Engine Settings，**不动 GPU**。
4. 镜里手/爪挡脸（第5镜就是） → **sync-3**（$0.107–0.133/s，内置遮挡检测）或 **KeySync Advanced(SAM2)**。

### P2 · 1–2 周，本地化降本
5. 本机 ComfyUI 装 **WAN2.2-S2V（fp8/GGUF）**（复用另一个会话正在做的 Wan2.2 双专家 + 显存让位基建）；
   "运镜已定稿"的镜用 **InfiniteTalk v2v**；批量/预览档用 **EchoMimicV3**。

### P3 · 中期正解：jaw-lip 解耦（见 §4）

---

## 4. jaw-lip 方案详解（音频分别驱动「下颌」与「嘴唇」）

### 4.1 核心思想
把"说话时的面部运动"拆成两个独立通道：

| 通道 | 驱动信号 | 语义 | 对应 "喊" |
|---|---|---|---|
| **Jaw（下颌）** | 韵律/prosody：响度、音高、时长 | 嘴巴"张多大" | **喊叫 = 响度大 + 长元音 → 下颌大开** |
| **Lip（嘴唇）** | 音素/viseme | 嘴唇"什么形状"（闭/圆/展/齿唇） | 喊叫中仍会出现闭唇、圆唇音 |

这就是 **JALI** 的原始设计（JALI = **Jaw And Lip**），VisemeNet 也把「说话风格（含 shouting）」显式建模为整脸 landmark 的运动。
**现有嘴部替换模型把两者混在一张 mask 里**，所以一旦要"下颌大开"，就只能靠像素层硬猜 → 这就是画面被破坏的根因。

实用推论（很关键）：**"张嘴"和"闭唇"必须能同时成立**——例如"张着下颌喊 `m`/`b`"、
"大喊中的圆唇 `o`"。NVIDIA Audio2Face 的做法是把 `JawOpen` 与 `MouthClose` 当两个独立 blendshape 叠加。

### 4.2 三条落地路径

#### J1（推荐先做，1–2 周）：**EchoMimic（可编辑 landmark）+ 我们自己的下颌曲线注入**
EchoMimic V1 的卖点就是「**Editable Landmark Conditioning**」：先由音频生成 landmark（含下颌轮廓），
**landmark 可编辑**，扩散模型再照着 landmark 出干净画面；V2 增加 Audio-Pose 动态协调；V3（1.3B）统一多任务且只需 12–16 GB。

流程：
```
配音 wav ─┬─► viseme/landmark 模型（EchoMimic 自带）─► landmark 序列 L(t)
          └─► 响度包络 loudness(t) ─► jaw_open(t) ──► L'(t)：下唇/下巴顶点 y 位移放大 k·jaw_open(t)
                                    └─► 唇形约束（闭唇/圆唇优先于下颌）
L'(t) + 参考静帧 ─► 扩散 ─► 画面（干净、下颌真的开了）
```
- `jaw_open(t) = clip(σ(w·loudness_norm(t)+b)·gain_emotion, 0, 1)`；响度用 50 ms 窗 + attack 30–60 ms / decay 100–150 ms 平滑
  （喊叫是"快起慢落"）；长元音（/a/ /e/）额外加权；`gain_emotion` 就是给美术的"喊叫强度"滑杆。
- 合成规则：`jaw = max(viseme_jaw, prosody_jaw)`、`lips = viseme_lips`，**闭唇类音素优先**（避免"张着嘴发 m"）。
- 牙齿/口腔：下颌大开必须露上牙（必要时下牙/舌）→ 用"口腔内层"资产或一个小的 inpainting 分支按 `jaw_open(t)` 强度合成。
  **这是多数 bad dub 的破绽，必须在验收里单独看。**
- 审计：把 `gain/attack/decay/seed` 写进任务 payload（与现有 `prompt_md5` 同一思路）；曲线可视化给美术微调（复用现有配音时间轴 UI）。
- 验收：直接用 §3 的包络指标（峰值比 + 相关性 + 下颌区时域差）。

#### J2（2–4 周）：**3DMM / FLAME 参数层**，两条出口
- 参数来源（已有开源可复用）：
  - **STEER**（SIGGRAPH Asia 2026）：61-D FLAME 状态（expression, **jaw**, neck, eye rotation, eyelids），causal flow-matching，可显式控制 gaze/head rhythm/emotion；
  - **UniTalker**（ECCV 2024）：统一多数据集、多标注输出；
  - **LipShapeModel**：开源「FLAME 参数 + 音频 + 文本 → 106 维口型序列」训练/推理；
  - GaussianFlameTalk：audio-driven Gaussian Splatting，实时且时域稳定。
- 出口 A：参数 → 驱动 3D 头渲染 → 与 plate 合成（需要 face fitting + comp，工作量大、上限高）。
- 出口 B（更推荐）：参数 → 转成**几何条件**（landmark / 深度 / 法线）→ 喂给扩散模型，
  等于"3D 指导的 2D 生成"——比 J1 更能保证下颌与牙齿的物理正确性。

#### J3（重，长期）：**真 3D rig / 表演层**
`Audio2Face-3D`（NVIDIA 已开源：模型 + SDK + Maya/UE 插件 + 训练框架；NIM 可输出 **ARKit blendshape**，支持显式 emotion）
→ 驱动 MetaHuman/自建 rig → 离线渲染 → 合成回 plate。质量与可控性最高，但需要角色 3D 资产与渲染管线，短期不做。

### 4.3 为什么值得做（相对"换整脸生成"的取舍）
| | 整脸生成（S2V / Veo / InfiniteTalk） | jaw-lip 解耦 |
|---|---|---|
| 喊叫表现力 | 强（整脸重演） | 强（下颌可控） |
| 与既有画面/运镜一致 | 会重演镜头（运镜/背景可能变） | **复用既有画面**（只改表演） |
| 身份稳定性 | 需额外锚定（重画脸） | 天然稳定（复用贴图/几何） |
| 美术可控性 | 提示词级 | **参数级**（喊叫强度滑杆、闭唇优先） |
| 成本 | 本地重（14B 级）/ 云按秒 | 本地轻（1.3B 级起步） |
| 工作量 | 接入快（现成模型/API） | J1 ~1–2 周，J2 ~2–4 周 |

**组合建议**：喊叫镜优先走"整脸生成"救急（P1）；同时把 **J1** 做起来，作为长期"表演层"——两者共用同一套包络 QC，可 A/B 对比。

---

## 5. GPU 服务器「加存储」的可行性（附体积预算）

### 5.1 现状（据 `docs/gpu-newserver-models.md`，2026-09-13 实测）
- 硬件：AMD EPYC 7542 128 核 / **503 GiB RAM** / **RTX 4090 24 GB**（驱动 580.65.06、CUDA 13.0）。
- 磁盘：模型放 `/home/dataset-local`（平台数据盘），**当前 100 GB**；已规划模型 **≈50.1 GiB**（ComfyUI 核心 27.68 + ACE-Step 9.34 + CosyVoice 6.0 + LatentSync 6.9 + whisper/face_id 0.16）→ **余量约 50 GB**。
- 下载：ModelScope 单连 3.24–3.95 MB/s，10 并发 8.3 MB/s（50 GiB ≈ 1.7 h）；本机→服务器上传仅 0.23–1.37 MB/s（**不要靠上传搬模型**）。

### 5.2 本方案要新增多少（已核实权重体积）

| 项 | 净增 | 备注 |
|---|---|---|
| Wan2.2-I2V-A14B（另一会话在建） | **≈26 GB** | fp8 双专家各 13.3 GiB |
| Wan2.2-S2V-14B | **≈17 GB** | `wan2.2_s2v_14B_fp8_scaled` = **16.4 GB**；umt5(6.27G) 复用；+wav2vec2/VAE ~1 GB |
| InfiniteTalk（基础） | **≈20 GB** | Wan2.1-I2V-14B fp8 + InfiniteTalk 权重（single/multi 各 ~2.7 GB） |
| EchoMimicV3 | **≈10 GB** | 权重仓 7.14 GB + 1.3B base + wav2vec2 |
| HunyuanVideo-Avatar（若上） | +15~26 GB | 13B，fp8/fp16 |
| KeySync（若上） | +5~8 GB | 直接替代 LatentSync 的实验 |
| **合计（不含后两项）** | **≈73 GB** | 需要 **≥120 GB 可用**才能宽松 |

**结论**：100 GB 数据盘**不够**；建议数据盘 **≥200 GB**（留 80–100 GB 余量给多套量化权重/兜底/缓存），
若要走"全量下载"（含 fp16 兜底与 ACE 原生组件，参考社区那套 228 GB 清单）→ **300–500 GB**。
下载时间：73 GB @ 8.3 MB/s ≈ **2.4 h**（可后台并行；与装环境重叠）。

### 5.3 怎么加（三条路，按省事排序）
1. **平台控制台扩容数据盘/加挂数据盘**（推荐）：多数平台支持"数据盘/数据集"调整大小或新建挂载；
   若只能在创建实例时选容量，则**重建实例**（把模型目录做成持久盘挂载 + 一键重装脚本）。
2. **对象存储 + 按需软链**：冷模型放对象存储，用时拉取（省盘但费时间；下载带宽 8.3 MB/s，16.4 GB 的 S2V 约 35 min）。
3. **不扩盘，走云 API**：Veo 3.1 / wan-2.2-s2v / sync-3 按镜付费 → **本地存储与显存压力都消失**（适合先把质量/成本验证出来）。

### 5.4 关键提醒：**存储不是真瓶颈，24GB 单卡显存才是**
- S2V-14B fp8（16.4 GB）+ umt5(6.3) + VAE(1.3) + wav2vec2(0.6) ≈ **24.6 GB > 24 GB**：必须量化更狠（GGUF Q4–Q6）、
  分块/offload，否则 OOM；InfiniteTalk（Wan2.1-14B）同理。
- 24G 单卡不可能"同时"挂 14B 视频模型 + LatentSync/TTS → 必须**串行 + 显存让位**（另一个会话正在做的 `--disable-smart-memory`/`_yield_vram_for_video` 是前置条件）。
- 若要"本地顺畅跑满全量"，真正的升级路径是 **加第二台 24G 做并行** 或 **换 48G/80G 卡**；否则维持"本地轻档（EchoMimicV3 1.3B / LatentSync）+ 重档走云"的组合最经济。
- 风险：容器会轮换/重建（SSH 端口已变过一次，本次排查期间 `36.103.182.217:30203` 一度拒连）→
  **模型必须落在持久数据盘**，并维护 `deploy/` 下的一键重装脚本（模型清单 + 校验和 + 软链）。

---

## 6. 验证协议（把"效果不可接受"变成可量化验收）

- **样本**：第5镜（大张口底片 + 闭嘴底片）+ 第6镜 + 2 个合成喊叫（正脸 / 侧脸 / 遮挡各一），同一条配音。
- **A/B 组**：现状 LatentSync（闭嘴底片基线）/ LatentSync+自适应 mask+更高工作分辨率 /
  wan-2.2-s2v(云) / Veo 3.1(云) / InfiniteTalk(v2v) / EchoMimicV3 / **分镜规避（不给正脸）** / jaw-lip(J1)。
- **指标**：① 张口包络相关性 ② 峰值张口（当量）③ 下颌区时域差分（缝合/闪烁）④ ArcFace 身份相似 ⑤ 3 人盲评两问："像不像在喊"、"有没有坏"。
- **通过线**：盲评 ≥2/3 通过 且 ①≥0.5 且 ③ 不超过普通镜的 1.5×。
- **预算**：云路线 4 条 5s ≈ 十几分钟 + **$1–3**；本地每模型环境 0.5–2 天 + 显存适配（J1 另计 1–2 周）。

---

## 7. 风险与坑

- 整脸生成会**重演镜头**（运镜/表演/背景）→ 与"关键帧两段式 + 运镜定稿"体系冲突，必须**按镜选路线**。
- 身份漂移（生成模型重画脸）→ 需定妆照/参考图锚定 + 跨镜 ArcFace QC。
- 已知问题：InfiniteTalk（源视频退化 / 运行时显存压力）、KeySync（身份/牙齿）、LivePortrait 的 `lip_retargeting` 仍是 WIP（社区反馈参数有时被忽略）→ 不要单点依赖。
- 云：Sora 2 不要押（API 关停的公开报道）；Veo/s2v/sync 价格随时变 → 落地前按 provider 页面核价。
- 成本纪律：建议**只把"喊叫 / 吟唱 / 遮挡"三类镜走云**，普通对白继续本地 LatentSync。

---

## 8. 待办（等批准，未执行）

1. 本文件评审 → 定 P0/P1 是否立即做。
2. P0：`DirectorPlanValidator` 加"喊叫镜禁止正脸近景"硬规则 + 包络 QC 指标（`mouth_envelope` 相关/峰值）。
3. P1：云 A/B（4 条短片）→ 决定整脸生成走 Veo 3.1 还是 wan-2.2-s2v。
4. P2/J1：EchoMimicV3 环境 + 下颌曲线注入层（含曲线可视化与审计字段）——**落地机与逐步步骤见 §9（GPU#2 已实测确认）**。
5. §5：确认数据盘是否可扩容到 ≥200 GB；把新模型清单并入 `deploy/` 一键重装脚本（持久盘 + 校验和）。

---

## 9. GPU#2 落地确认与执行步骤（2026-09-14 深夜实测）

> 目标机：**GPU#2** `180.127.11.167:15216`（ssh 别名 `weaveora-gpu-a14b`，hostname `ubuntu24`，Ubuntu 24.04）。
> GPU#1 `36.103.182.217`（24G）**已关机**；生产服务已切到 GPU#2 网关 `http://180.127.11.167:15264`。
> 本文档 §5 的“加存储”分析是针对 GPU#1 的 100 G 数据盘写的，**GPU#2 的数字见本节**。

### 9.0 机器档案（实测）

| 项 | 实测值 | 对 jaw-lip 的含义 |
|---|---|---|
| GPU | RTX 4090 **48 GB**（`vram_total` 47.4 GiB，空闲时 46.5 GiB） | ✅✅ 比 24G 宽松得多；EchoMimicV3（12–16G）/S2V-14B fp8（16.4G）/InfiniteTalk（≈20G）都能装下 |
| CPU / RAM | 16 核 / 50.4 GB（空闲 ≈25.9 GB） | ✅ 够（音频曲线/landmark 处理都是轻活） |
| 磁盘 | `/dev/vda1` **196 G，已用 111 G，余 75 G**（单文件系统，`/opt` 与 `/home` 同盘） | ⚠️ 够 J1（约 18 G），但加 S2V/InfiniteTalk 要算着用 |
| 目录布局 | **`/opt/weaveora`**：models 44 G（diffusion_models 27 G = A14B 双专家 fp8、checkpoints 9.4 G、text_encoders 6.3 G、loras 1.2 G、vae 243 M）、ComfyUI 8.5 G、latentsync 6.6 G、envs 6.3 G、audio 6.1 G | 与 GPU#1（`/home/dataset-local`）完全不同 → 脚本要用 `WEAVEORA_ROOT` |
| 服务 | ComfyUI `:8001`（`--disable-smart-memory --reserve-vram 0.5`）、TTS `:8091`（`envs/cosy`）、face `:8093`（CPU）、edge_proxy `:8800` → 平台网关 `:15264` | ✅ 同一台机器上已跑齐 tts/face/bgm/lipsync/comfy |
| 框架 | ComfyUI **0.34.0**、Python **3.12.13**、PyTorch **2.7.1+cu126** | ✅ 正是 GPU#1 踩坑后定下的避坑版本（避开 torch 2.14 + DynamicVRAM 死锁） |
| LatentSync 节点补丁 | **2026-09-14.1**，能力齐全（`point_lock/inline_spec/track_lock/quality_gate/paste_mask/fps_pin/debug_box`） | ✅ 现有对口型在 GPU#2 可直接跑（worker 的节点强校验会过） |
| face 服务版本 | 已含 `face_px` / `mouth_open`（源码 17 处命中） | ✅ 我们的底片体检与包络 QC 指标在 GPU#2 可用 |
| custom_nodes | **只有** `ComfyUI-LatentSyncWrapper` | 新能力要么装节点、要么走独立服务 |
| Python 环境工具 | ComfyUI venv **没有 pip**；系统有 `python3.12` + `venv`；`uv` 已存在（`/opt/miniconda3/bin/uv`、`/root/venv/bin/uv`）；apt 可用 | 用 `uv` 建独立 venv（不要动 ComfyUI 的 env） |
| 网络 | modelscope API 端点 ≈6.9 MB/s、`hf-mirror` / `aifasthub` / `github` 均 200；pypi 镜像 301→可达 | ✅ 权重与依赖都能拉 |
| 驱动健康 | ⚠️ `nvidia-smi` 报 **NVML driver/library version mismatch**（内核模块 595.71.05 vs 用户态库 595.84）；但 **torch 侧 CUDA 正常**（`torch.cuda.is_available()=True`、ComfyUI 报 46.5 GiB free） | ⚠️ 计算不受影响；但所有依赖 `nvidia-smi`/pynvml 的监控或脚本会挂 → 建议平台重启实例修一次（**非阻塞**）。显存判断请改用 ComfyUI `/system_stats` 或 torch |

### 9.1 可行性逐项确认

| 维度 | 结论 | 说明 |
|---|---|---|
| 显存 | ✅ **可行且宽裕** | EchoMimicV3（1.3B，12–16 G）在 48 G 上很轻松；甚至可以直接上 S2V-14B fp8（16.4 G）或 fp16 做 A/B |
| **与 A14B 出片互斥** | ⚠️ **必须串行** | A14B 双专家出片实测峰值 **41.8–42.4 GiB**，与 talking-head 模型（16 G）**不能同时驻留 48 G** → 出片前 `/free` 卸载 + 队列互斥（复用 `_yield_vram_for_video` 思路，加“模型家族切换前卸载”） |
| 磁盘 | ✅ 够 J1 | J1 = 模型 ≈10 G + 独立 venv ≈8 G → 余 ~57 G；若再加 S2V（+17 G）/InfiniteTalk（+20 G）仍可行，但要先清 HF 缓存与 `_dl` |
| 依赖 | ⚠️ 坑已识别 | EchoMimic 官方 `requirements.txt` 里有 **`tensorflow==2.15.0` + `retina-face`**（与 numpy 2 / 现代 diffusers 栈冲突）→ **绝不能装进 ComfyUI 的 venv**；用 `uv` 建独立 venv，TF/retina-face **按需再装**（裁脸可改用已有的 insightface 替掉 retina-face） |
| 网络/权重 | ✅ | EchoMimicV3 走 hf-mirror（`BadToBest/EchoMimicV3`，≈7.14 G）；base `Wan2.1-Fun-V1.1-1.3B-InP` 走 ModelScope（≈2.6 G）；`wav2vec2-base-960h`（≈0.4 G） |
| 管线接线 | ✅ 低成本 | 服务模式可完全照抄 `/opt/weaveora/audio/tts_server.py`（独立 venv + HTTP + 网关路由）；worker 侧新增一个 kind 即可 |

**结论：GPU#2 上落地 jaw-lip 可行，且比蓝图写的 24 G 场景舒服得多**；三个前置待办：① `nvidia-smi`/NVML 修一次（平台重启，非阻塞）② 用 `uv` 建独立 venv（不动 ComfyUI env）③ 与 A14B 出片做显存串行。

### 9.2 执行步骤（建议顺序：**先验证模型，再做我们的曲线层**）

#### S0 · 机器自检与档案固化（≈10 min）
```bash
R=/opt/weaveora
nvidia-smi                                   # 仍 mismatch → 请平台重启（不阻塞后续）
curl -s localhost:8001/system_stats | head -c 300   # 显存口径以这个为准（NVML 不可用时）
df -h /; du -sh $R/models/* | sort -h | tail; ls $R/ComfyUI/custom_nodes
```
把上面的档案补进 `docs/`（不含密钥/密码），供换机后一键对照。

#### S1 · Spike A：**先用 EchoMimicV3 原生音频驱动跑第 5 镜（不做任何曲线注入）**（0.5 天）
这一步回答一个关键问题：**“整脸音频驱动”本身是否已经把“喊”做出来了**（下颌开合是它的输出，不需要我们注入）。
```bash
# 1) 独立环境（不碰 ComfyUI venv）
uv venv /opt/weaveora/envs/talk --python 3.12
uv pip install --python /opt/weaveora/envs/talk/bin/python \
  torch==2.7.1+cu126 torchvision --index-url https://mirrors.tuna.tsinghua.edu.cn/pytorch-wheels/cu126 \
  diffusers transformers accelerate safetensors librosa soundfile decord opencv-python imageio einops omegaconf numpy
# 2) 权重（≈10 G；落 /opt/weaveora/models/echo_mimic，与 ComfyUI 节点约定同布局，两条路都能复用）
#    transformer/diffusion_pytorch_model.safetensors  ← hf-mirror: BadToBest/EchoMimicV3
#    unet/ + vae/                                     ← ModelScope: alibaba-pai/Wan2.1-Fun-V1.1-1.3B-InP
#    audio_processor/                                 ← hf-mirror: facebook/wav2vec2-base-960h
# 3) 跑：官方 infer / app_mm.py（输入＝第5镜配音 wav + 新近景静帧）
```
**验收（打不过就换路线）**：产物 `mouth_open` 包络峰值 ≥ **0.9 当量**（对比：LatentSync 现状产物 0.443、原底片 1.238）
且与音频响度包络 **相关 ≥0.5**、下颌区时域差分不劣于普通镜 1.5×、ArcFace 身份相似不降；肉眼“像在喊 + 不坏”。

#### S2 · Spike B：**下颌曲线 + 后置下颌形变（最小闭环）**（0.5 天，仅当 S1 的“开合感”不够时才做）
```
配音 wav ──► 响度包络 loudness(t)（50ms 窗；attack 30–60ms / decay 100–150ms）
          ──► jaw_open(t) = clip(σ(w·loudness+b)·gain, 0, 1)          # gain = 美术“喊叫强度”滑杆
生成片段 ─► insightface 106 点/帧 ─► 下唇+下巴控制点位移（只动下半脸，颈部渐隐）
          ─► cv2 TPS/Delaunay 形变 + 张嘴时口腔内层 inpaint（露上牙；必要时下牙/舌）
```
指标增量就是验收指标（包络峰值提升、相关提升）。**关键提醒**：变形区只取“下唇~下巴”，避免“纸片人”拉扯；这是“往 2D 贴回”的路线，与 S1 的整脸生成互补。

#### S3 · 服务化（0.5–1 天）
把 S1/S2 包成 `/opt/weaveora/talk/talk_server.py`（照抄 `audio/tts_server.py` 的形态：独立 venv 启动、端口 **8094**、日志 + `/health`），
edge_proxy 加路由或由平台再映射一个端口；worker（VPS 侧）新增 `kind=talk`；EngineSettings `services` 增 `talk.url`。

#### S4 · 管线接线（1–2 天）
- plan：`shots[].talk_mode = 'jawlip' | 'off'` + `jaw_gain / jaw_attack / jaw_decay`（随 payload 落库，可审计，与 `prompt_md5` 同思路）；
- 路由：只给「喊叫 / 吟唱 / 遮挡」镜走 `talk`，普通对白继续 LatentSync；
- 互斥：`talk` 任务与 A14B 出片队列互斥；任务前 `/free` 卸模型；
- 资产：新 `kind=talk`（或复用 `lipsync` 但带 `source=talk`），渲染优先级 **talk > lipsync > clip**。

#### S5 · 验收与回退（按 §6 协议）
第 5 镜（大张口底片 / 闭嘴底片）+ 第 6 镜 + 2 个合成喊叫镜做 A/B：`LatentSync 现状` vs `EchoMimic 原生` vs `EchoMimic + jaw 曲线`；
结果回写本节；**回退开关** = 该镜 `talk_mode=off` 即回落 LatentSync。

#### S6 · 收尾
模型清单与校验和并入 `deploy/gpu2_*` 一键脚本；HF 缓存/`_dl` 定期清；若要再上 S2V(16.4 G)/InfiniteTalk(20 G)，先核 75 G 余量。

### 9.3 不要做的（避坑清单）
- ❌ 不要 `pip install -r requirements.txt` 到 ComfyUI 的 venv（无 pip，且 TF2.15/retina-face 会污染 numpy/torch 栈）；
- ❌ 不要与 A14B 出片并跑（42 G + 16 G > 48 G）；
- ❌ 不要用 `nvidia-smi` 作为显存判断依据（当前 NVML mismatch）→ 用 ComfyUI `/system_stats` 或 torch；
- ❌ 不要先花 1–2 周做曲线层再验证模型——**先 S1 原生跑通**，再决定曲线层要多大；
- ⚠️ 容器/IP/端口会轮换（本机 ssh 别名已从 `.166:10532` 变到 `.167:15216`）→ 一切改动都要能一键重装。
