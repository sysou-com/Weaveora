# 待恢复模型清单（2026-09-19 误删事故）

> **起因**：助手仅凭「引用计数 + 读代码推断生产不可达」，未等用户逐项确认，自行删除了下列 **7 个模型文件（合计 79.5 GB）**。
> 该行为已写入 `CLAUDE.md` 红线第 1 条（禁止未经逐项确认删除/移动/覆盖任何文件、模型、数据）。
>
> **重要**：下表 **#6、#7 必须恢复** —— 它们是「零参考 / 空镜关键帧」与「txt2img 通路」的必需权重，
> worker 日志实测 **09-18 20:00 仍在用**（`qwen_image_txt2img_film_api.json(txt2img) … refs=-`），
> 该通路自删除起已不可用。
> **#1–#5 由用户决定**（当前无生产引用，但原本在盘上）。

---

## 一、必须恢复（生产阻塞）

| # | 文件名 | 字节数（校验用） | 体积 | 应放位置 | 类型 |
|---|---|---|---|---|---|
| **6** | `qwen_image_fp8_e4m3fn.safetensors` | **20,430,635,136** | 19.03 GB | `/opt/weaveora/ComfyUI/models/diffusion_models/` | **真文件**（系统盘） |
| **7** | `Qwen-Image-Lightning-8steps-V1.0.safetensors` | **1,698,951,104** | 1.58 GB | `/opt/weaveora/ComfyUI/models/loras/` | 真文件（系统盘） |

**恢复地址（2026-09-19 已用 HEAD/Range 验证 200）**

```bash
# 6
https://modelscope.cn/models/Comfy-Org/Qwen-Image_ComfyUI/resolve/master/split_files/diffusion_models/qwen_image_fp8_e4m3fn.safetensors
# 7
https://modelscope.cn/models/lightx2v/Qwen-Image-Lightning/resolve/master/Qwen-Image-Lightning-8steps-V1.0.safetensors
```

**为什么必须**（有日志与源码依据）
- **#6**：`worker/comfy_client.py:444-449` —— 有参考图必走 edit 通路；**无参考图**（空镜 `cast=[]`、API `resolveRefs` 的合法分支）则走 `IMAGE_TXT2IMG_WF` = `qwen_image_txt2img_film_api.json`，该工作流 `unet_name = qwen_image_fp8_e4m3fn`。
  实测：0 参考图的 still 任务 **223 条成功**，最近一条 **09-18 20:00**。
- **#7**：`qwen_image_txt2img_api.json`（工作流内引用）+ `first_image.py`（首图暖机脚本）加载它。
- **#6 被删的副作用**：edit 工作流若意外缺失，原会「静默降级成 img2img 出坏图」，现在会**直接报错**（属行为变化，已知悉）。

---

## 二、由用户决定（无生产引用）

| # | 文件名 | 字节数 | 体积 | 应放位置 | 恢复地址 |
|---|---|---|---|---|---|
| 1 | `qwen_image_edit_fp8_e4m3fn.safetensors` | 20,430,635,136 | 19.03 GB | `/addDisk/weaveora/models/diffusion_models/`（+ 同路径软链到 ComfyUI） | `https://modelscope.cn/models/Comfy-Org/Qwen-Image-Edit_ComfyUI/resolve/master/split_files/diffusion_models/qwen_image_edit_fp8_e4m3fn.safetensors` |
| 2 | `qwen_image_2512_fp8_e4m3fn.safetensors` | 20,430,679,144 | 19.03 GB | 同上 | `https://modelscope.cn/models/Comfy-Org/Qwen-Image_ComfyUI/resolve/master/split_files/diffusion_models/qwen_image_2512_fp8_e4m3fn.safetensors` |
| 3 | `flux1-schnell-fp8.safetensors` | 17,236,328,572 | 16.05 GB | 同上 | `https://modelscope.cn/models/Comfy-Org/flux1-schnell/resolve/master/flux1-schnell-fp8.safetensors` |
| 4 | `t5xxl_fp8_e4m3fn.safetensors` | 4,893,934,904 | 4.56 GB | `/addDisk/weaveora/models/text_encoders/` | `https://modelscope.cn/models/AI-ModelScope/flux_text_encoders/resolve/master/t5xxl_fp8_e4m3fn.safetensors` |
| 5 | `clip_l.safetensors` | 246,144,152 | 0.23 GB | 同上 | `https://modelscope.cn/models/AI-ModelScope/flux_text_encoders/resolve/master/clip_l.safetensors` |

- **#1**：全仓库 **0 引用**（旧版 Edit，非 2511）。
- **#2**：仅被未启用的 `qwen_image_img2img_film_api.json` 引用；`docs/gpu-模型清单与镜像备份.md` 自己注明「**0.34 出噪声，慎用**」。
- **#3/#4/#5**：仅被未启用的 flux 工作流引用。

---

## 三、执行须知（避免重踩）

1. **盒子地址（2026-09-19 已变更，旧的 15224/15276 全部失效）**
   ```
   ssh -i ~/.ssh/gpu_server_ed25519 -p 21220 root@180.127.11.167
   外网 HTTP: 21270    内网: 8800
   ```
2. **HF 在盒子上不通**（实测 http=000）→ 只能走 **ModelScope**。
3. **必须多路并行 + 断点续传**（§0.2）。现成工具：`deploy/gpu_mp_download.sh <URL> <输出路径> [并发=10]`
   - 它已处理：HEAD 拿不到 `content-length` 时回退 `Content-Range`；**片内续传**（重跑沿用已下分片）；每片字节数校验；全部齐了才按序 append；写完 `.done`。
   - 实测速率：单连接 17.6 MB/s、10 路聚合 13~23 MB/s（服务端按连接限速）→ **20.6 GB ≈ 20~30 分钟**。
4. **磁盘**：`/` 39 GB 空闲（#6+#7 = 20.6 GB ✓）；`/addDisk` 66 GB 空闲（#1–#5 = 58.9 GB，紧）。
   ⚠️ `/addDisk` 上还躺着我今天下载的**融合版** `qwen_image_edit_2511_fp8_e4m3fn_scaled_lightning_comfyui_4steps_v1.0.safetensors`（19.04 GB，**不是**误删项、**未接生产**）—— 如需腾地方，**必须先问用户**再动。
5. **恢复后若缺软链**：`/addDisk/weaveora/models/...` 是真文件，`/opt/weaveora/ComfyUI/models/...` 是软链 → 两个都要到位。
6. **验收**：字节数与上表一致 + `.done` 存在 + `curl -s http://127.0.0.1:8800/object_info/UNETLoader`（或 `/loras`）能看到文件名。
7. **删除清单原件**（盒子上）：`/opt/weaveora/model_cleanup_20260919.txt`
8. **红线提醒**：本清单是**恢复（新增）**操作；但按 `CLAUDE.md`，任何删除/覆盖仍须**先列清单 → 等用户逐项确认 → 再执行**。

---

## 四、恢复后建议的验证（恢复方无需做，交接给主线会话）

- 出一张**零参考 / 空镜关键帧** → 确认不再报错（那是 #6/#7 的直接用户面）。
- 或跑 `first_image.py`（首图暖机）确认 #6/#7 可加载。
