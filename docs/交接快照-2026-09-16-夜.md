# 交接快照 · 2026-09-16 夜（P13b / P14：定妆照、位置总控、提示词一致性）

> **这是最新的一份，开新会话先读它。** 紧接着读 `docs/知识档案索引.md`（按场景挑后续文件）。
> 白天那份 `docs/交接快照-2026-09-16.md`（§1–§9）保留为**明细档案**（模型 sha256、逐条排查过程、GPU 指南索引）；
> 本文件是**自足版**：环境、部署、链路现状、数据模型、代码地图、待办、纪律一次说清，不读旧文件也能开工。
> 纪律见 `Weaveora.md §0.3 上下文与输出纪律`（按任务开新会话 / 禁止倒原始日志 / 禁止轮询循环 / 大文本走 ctx / 长输出先落盘）。

---

## 1. 一分钟状态（2026-09-17 13:0x 刷新；这天是跨机协作日）

| 项 | 值 |
|---|---|
| 仓库 HEAD | `1ffc29a`（剧本模块 6 + 跨机伙伴 4 + 本机 2）；`origin/master` 由本机于 **2026-09-17 13:0x 一次推全**（此前长期停在 `7f2a9cc`） |
| 线上 API | jar `92,095,428 B`（**09-17 12:24**，含剧本模块 + 生成前置闸门 `requireEngineOnline`）；回滚点 `weaveora-api.jar.bak.20260917-123557` |
| 线上 Web | bundle **`index-aPeIinFt.js`**（09-17 12:27，剧本模块「集→项目 / 查看项目 / 保存提速」版） |
| 线上 Worker | `comfy_client.py` / `stub_worker.py` = 09-17 00:05 版（含：修显存误判降帧、自动降分辨率保时长、notes 上报） |
| 任务队列 | 空。**00:20 那批 5 条 clip 全部 `COMFY_ERROR timed out` 失败**（盒子中途掉线）→ GPU 恢复后用任务区「重试选中」批量重跑 |
| GPU#2 | 🚨 **09-17 09:30 起：地址指向的不是我们的机器** —— `180.127.11.167:31058` 现在是 ComfyUI **0.35.0** / RAM **50.5G** 的实例，模型只有 `krea2_turbo`、`minimax_h3_*`、`qwen3vl_32b`（**我们的 qwen_image_2512/2511、wan2.2_a14b、qwen_2.5_vl_7b 全无**），`/__edge/health`+`/audio*`+`/face*`+`/talk*` **全 404**，SSH `31012` 能连但**我们的 key 被拒**。已止血：停 `weaveora-gpu-worker` + 新增生成前置闸门（无在线节点 → `WORKER_UNAVAILABLE(503)`）。**用户裁定：GPU 暂时不管**，等平台给回我们实例的地址/端口/SSH 授权 |
| 本轮变更 | 两个 commit：`7913aa8`（P13b：定妆弹框/新增主体/位置总控/参考图语义/第4镜错位真因）、`955b7c2`（P14：设定年代 + 主体档案） |
| 待办 3 已做 | **2026-09-16 夜**：3 个有主体的生产方案补齐 `setting.era` + 人物档案（走产品接口，只补空、幂等）→ `deploy/backfill_setting_traits.{py,json}`；免 GPU 验证 `--print-portrait` |

---

## 2. 环境与访问

| 项 | 值 |
|---|---|
| VPS（API + worker + 静态站） | `root@sysou.com`（本机默认私钥可登，**无需** `-i`）；服务 `weaveora-api` / `weaveora-cloud-worker` / `weaveora-gpu-worker` |
| GPU#2（ComfyUI/TTS/face/talk） | `180.127.11.167`（机器名 `ubuntu24`）；网关 `http://180.127.11.167:31058` → 机器内 `edge_proxy.py :8800`；SSH **`-p 31012`**（2026-09-16 改为 31012；别名 `gpu`/`gpu2`/`weaveora-gpu-a14b` 均已同步）。**端口每次平台重开都可能变 —— 以平台「生成引擎配置 → GPU 服务器地址+端口」为准，不要从文档推断** |
| 权威来源 | 平台「生成引擎配置 → GPU 服务器地址+端口」（DB `user_engine_settings.services`），**不要**从文档推断端口 |
| VPS 路径 | API `/opt/weaveora/api/`；worker `/opt/weaveora/`；web `/opt/weaveora/web/`；工作流 `/opt/weaveora/workflows/`；交付 `web/_share/` |
| 数据库 | VPS 上 PostgreSQL，库 `weaveora`（`sudo -u postgres psql -d weaveora`） |
| 本机部署前置 | 私钥 `~/.ssh/id_ed25519`；JDK21 `C:\Program Files\Eclipse Adoptium\jdk-21.0.12.101-hotspot`（两个脚本默认值都失效过，已加「存在即用」自动探测） |

---

## 3. 部署（三个脚本 + 回滚）

```bash
cd /d/workspace/Weaveora

# ① API（含 mvn clean package + 测试；有 running 任务会拒绝重启，FORCE=1 强推）
JDK21="/c/Program Files/Eclipse Adoptium/jdk-21.0.12.101-hotspot" \
WEAVEORA_SSH_KEY=/c/Users/wanglg/.ssh/id_ed25519 bash deploy/api-deploy.sh

# ② Web（type-check + build:prod + 原子替换 index.html）
WEAVEORA_SSH_KEY=/c/Users/wanglg/.ssh/id_ed25519 bash deploy/web-deploy.sh

# ③ VPS worker（传 .new → 用**目标机的 py3.6** 语法校验 → 原子改名 → 重启两个 worker）
WEAVEORA_SSH_KEY=/c/Users/wanglg/.ssh/id_ed25519 bash deploy/vps-worker-deploy.sh
```

**回滚**

| 层 | 命令 |
|---|---|
| API | `ssh root@sysou.com 'cd /opt/weaveora/api && mv weaveora-api.jar.bak.<ts> weaveora-api.jar && systemctl restart weaveora-api'` |
| Web | `ssh root@sysou.com 'cd /opt/weaveora/web && mv index.html.bak.<ts> index.html'` |
| Worker | 把每个 `<file>.bak.<ts>` 移回，再 `systemctl restart weaveora-cloud-worker weaveora-gpu-worker` |

**本机坑（都踩过）**

- `deploy/*.sh` 原来写死的 `comfy_tunnel_ed25519` 与 `/d/jdk21/jdk-21.0.2` 在本机**不存在** → 已加自动探测；仍建议显式传参。
- VPS 是 **Python 3.6**：远程辅助脚本里 `subprocess(..., text=True)` / f-string 会挂，用 `universal_newlines=True`。
- `api-deploy.sh` 必须 `mvn clean package`（不 clean 会把改名/删除过的资源如 Flyway 迁移留在 jar 里 → 启动失败）。
- **GPU 侧服务不要因为一次代码部署去重启**；GPU 重启另有纪律（见 §9）。

**免交互验证新接口的办法**（本轮用过）：在 VPS 上用 `WEAVEORA_JWT_SECRET`（`/etc/weaveora/weaveora-api.env`）手签一个 HS256 token
（claims：`sub=<userId>`、`type=access`），请求头带 `Authorization: Bearer …` + `X-Workspace-Id: <wsId>`。
⚠️ 用**一次性项目**（如 `unlock-test`）验证，写完把造的数据清掉。

---

## 4. 出图链路现状（结论优先）

### 4.1 引擎配置（DB 里当前生效，配置页可改，代码不写死）

```jsonc
services.image = {
  engine: "comfy",
  comfyUrl: "http://180.127.11.167:31058",
  workflow:        "/opt/weaveora/workflows/qwen_image_txt2img_film_api.json",  // 2512 文生图
  editWorkflow:    "/opt/weaveora/workflows/qwen_image_edit_api.json",          // 2511 参考图锚定
  img2imgWorkflow: null,                                                        // 故意留空（会把白底带过来）
  steps: 40, cfg: 4.0, denoise: 1.0
}
```

### 4.2 Edit 工作流接线（官方结构，勿打乱节点 id 顺序）

- `LoadImage` **3 个槽**：节点 `12`/`14`/`16` → 按 **id 字符串排序**依次喂 `image1/2/3`。
- `12 → ImageScale(目标尺寸, crop=disabled) → VAEEncode(节点 15) → KSampler.latent_image`（采样起点 = ref1 的 latent，`denoise=1.0`）。
- `TextEncodeQwenImageEditPlus`（正 `6` / 负 `7`）同时接 `vae + image1/image2/image3`。
- `ModelSamplingAuraFlow.shift = 3.1`（官方 2511 唯一参数差异）。
- worker 注入：`6.prompt / 7.prompt / 12.image / 14.image / 16.image / 13 尺寸 / 9.seed/steps/cfg/denoise`。

### 4.3 关键结论（都是实测/源码核对过的，别再重复踩）

1. **槽位命名要用模型自己的 `Picture N`**：`TextEncodeQwenImageEditPlus` 内部拼的是 `Picture 1: <|vision_start|>…`；只写 `image1` 时模型未必对得上 → 多主体张冠李戴。现在**两种并列写**：`Picture 1 (image1) = 宝玉[…]`。
2. **位置走「提示词」（路线 A）**。区域条件（`ConditioningSetAreaPercentage` 包参考图 conditioning）在 Edit 档会 `IndexError: tuple index out of range`，是 ComfyUI 核心限制，**不是接线错**；`WEAVEORA_IMAGE_AREA_COND=1` 才启用（默认 0）。
3. **Reference image 上传失败必须中止任务**：旧逻辑失败就跳过 → 后面的图前移一格而正词里的槽位号不变 → 角色脸互换。现在 `ComfyError` 直接失败并打印 `[comfy] 参考图槽位映射：Picture 1 = …`。
4. **Qwen-Image-Edit 对负词不敏感**（架构上没做 CFG 训练）：要"不要白底"就写"背景是…"，别堆负词。worker 仍会按语言追加白底/证件照类负词（代价低）。
5. **中英混排会两头听**：worker 会往正词前追加"怎么用参考图"的指令，现已**跟随提示词语言**（`_looks_zh()`，与后端 `isZhText` 同口径）。
6. **参考图 ≠ 锚定图**：上传素材图只作为"生成/指定定妆照"的输入；出图锚定**只认定妆照**（`subjects[].portraitAssetId`），没有定妆照的主体不注入（主主体缺则报错中止）。

### 4.4 ★ 出图正词的最终拼装顺序（改提示词前必看）

`still` / `clip`（Edit 档，有参考图）最终正词，从上到下：

| # | 片段 | 谁写的 |
|---|---|---|
| 1 | `参考图按送入顺序对应片中角色（Picture 1 Picture 2…）。… 场景：` / 英文同义句 | **worker**（`comfy_client.generate_via_workflow`，语言跟随） |
| 2 | 风格模板 prefix | `styledPositive()`（StyleTemplate） |
| 3 | `shot.positive_prompt` | LLM / 用户 |
| 4 | `Action: <shot.action>`（**仅 clip**） | `videoShotPayload()` |
| 5 | 身份约束句（每个角色只用自己的参考图，禁止混合/互换面容） | `refs.anchor()` = `refAnchor()` |
| 6 | `【设定年代/世界观】<era>…`（英文提示词则 `[Setting / era] …`） | **`JobService.applySetting()`**（P14，幂等） |
| 7 | `Picture N (imageN) = 主体[gender/age/build/look]（方位, x, y, 框 w×h）；… 【位置以本清单为准】` | **`JobService.applyLayoutRegions()`**（P13/P14） |

负词：`style.negative + shot.negative_prompt` →（clip 再加 `MOTION_NEGATIVE`）→ `applySetting` 的年代负词 → worker 的 Edit 档白底负词。

---

## 5. 数据模型速查（方案 JSON，`prompt_revisions.schema_json`）

```jsonc
{
  "mode": "video",
  "title": "…", "logline": "…",
  "setting": { "era": "清代 · 康熙年间", "notes": "服化道按清代贵族宅院形制" },   // ★ P14

  "subjects": [{
    "name": "宝玉", "kind": "person",            // person|vehicle|object|scene
    "aliases": ["贾宝玉", "宝二爷"],
    "enabled": true, "locked": false,
    "refs": [{ "assetId": "…", "checked": true, "region": {x,y,w,h} }],  // 素材图（只作定妆依据）
    "portraitAssetId": "…", "portraitVersion": 2,                        // 定妆照 = 唯一锚定物
    "region": { "x": 0.24, "y": 0.08, "w": 0.19, "h": 0.76 },            // 方案级默认位置
    "gender": "male", "age": "17", "height": "178cm",                    // ★ P14 人物档案
    "build": "清瘦", "personality": "多情敏感", "appearance": "大红箭袖，项上金螭璎珞"
  }],

  "shots": [{
    "shot_no": 4, "action": "…", "positive_prompt": "…", "negative_prompt": "…",
    "cast": ["宝玉", "可卿", "警幻"],              // 本镜主体；[] = 明确空镜；缺省 = 按文本自动
    "layout": [{ "subject": "宝玉", "x": 0.24, "y": 0.08, "w": 0.19, "h": 0.76 }],  // 镜级位置
    "keyframes": [{ "label": "结束帧", "composition": "…", "positive_prompt": "…",
                    "cast": ["宝玉"],              // ★ 帧级剧情主体（缺省=继承 shots[].cast；[]=该帧空镜）
                    "layout": [ … ] }],            // 帧级位置（优先级最高）
    "lipsync_targets": { "宝玉": { "x": 0.3, "y": 0.4 } }
  }]
}
```

**位置优先级**：`keyframes[j].layout`（帧） > `shots[].layout`（镜） > `subjects[].region`（方案默认） > `refs[].region` > `lipsync_targets` 点选（默认框 0.30×0.45）。
**主体优先级（2026-09-16 夜新增）**：`keyframes[j].cast`（帧） > `shots[].cast`（镜） > 按文本自动匹配（匹配文本 = 镜 action/zh/positive_prompt **+ 该帧的 composition/positive_prompt/label**）。
> 为什么要帧级 cast：运镜镜头的第 2..N 帧常常换了视角/换了主体，以前只有镜级 cast → 那些帧只能“继承 + 猜”，出图随意（用户实测反馈）。UI 入口：分镜卡 →「运镜关键帧」每一帧的「本帧主体」勾选 + 「编辑本帧区域」（直达位置总控的「第N镜·帧M」作用范围）。
**归一化 0–1**；`w/h` 越大 = 越近越大；后端把结果写成 `referenceRegions`（与 `referenceKeys` 同序）**并**拼进正词。

---

## 6. 代码地图（改哪里）

| 关注点 | 文件 |
|---|---|
| 提示词/一致性核心 | `api/.../job/JobService.java`：`resolveRefs` / `enforcePortraits` / `portraitOf` / `bindFromSubjects` / `applyLayoutRegions`（+`applySetting`）/ `videoShotPayload` / `createPortraitJob` |
| 提示词单一真源 | `api/.../director/SubjectPrompts.java`（定妆照正/负词、`portraitPrompt(name,kind,refCount,traits)`） |
| 主体档案模型 | `api/.../director/plan/PlanSubjects.java`（`Traits` 记录、扁平读写、别称归并、`traitsOf`） |
| 导演生成 / 重写 | `api/.../director/DirectorService.java`：`buildUserPrompt`、`settingAndSubjectBlock()`、`rewritePrompt`、`mergePrevSubjectsAndSetting()`、`patchSubjectMeta`、`portraitPromptDefaults` |
| 主体抽取（场记） | `api/.../director/SubjectService.java`（`ask()` 提示词要求档案；合并只补空） |
| 端点 | `api/.../director/api/DirectorController.java`（`…/revisions/{rid}/portrait-prompt`、`subjects/extract`、`subjects/meta`、`director/rewrite-prompt`）、`job/api/JobController.java` |
| LLM 系统提示词 | `api/src/main/resources/prompts/director_{video,image}_system.md`、`director_rewrite_shot.md`（+ `DirectorService.SYSTEM_FALLBACK` 内置兜底，两处都要同步改！） |
| GPU 出图 worker | `worker/comfy_client.py`（`generate_via_workflow`、`_wf_*` 注入、`_looks_zh`）；`worker/stub_worker.py` 认领任务 |
| 前端主界面 | `web/src/views/ProjectDetailView.vue`（参考图/剧情主体/位置总控/定妆弹框/主体设定弹框/差异摘要） |
| 前端方案编辑器 | `web/src/components/director/VideoPlanEditor.vue`（主题/设定年代、分镜列表）、`ImagePlanEditor.vue`、`ShotCard.vue`、`ShotLayoutEditor.vue` |
| 前端类型/接口 | `web/src/api/types.ts`、`web/src/api/{jobs,director,assets}.ts`、`web/src/utils/plan.ts` |
| 发版前前端自检 | `cd web && npm run check:components`（`web/scripts/check_template_components.mjs`）—— 扫「模板用了但没 import 的组件」；已挂在 `build` / `build:prod` 前 |
| P14 数据回填（既有方案补 era/档案） | `deploy/backfill_setting_traits.py` + `deploy/backfill_setting_traits.json`（在 VPS 上跑；`--dry-run` / `--force` / `--print-portrait 宝玉,可卿` / `--selftest-guard <一次性项目>`） |
| 部署后自检（定妆守门/参考图复制） | `deploy/selftest_ref_portrait.py`（VPS 上跑：① 零参考定妆任务必须被拒且不建任务行；② `as-reference` 复制成参考图 + 幂等 + 自清理） |

**改提示词的铁律：三处同口径** —— ①导演生成 + 单镜重写的 LLM 上下文（`settingAndSubjectBlock`）；②出图正词（`applySetting` + `applyLayoutRegions`）；③定妆照提示词（`SubjectPrompts`）。只改一处必然自相矛盾（历史事故就来自这里）。

---

## 7. 已完成（按 commit 索引）

| commit | 内容 |
|---|---|
| `ae0ff16`…`7b2c62f` | 位置三层（帧/镜/方案）+ 正词只写一遍 imageN + 主体锚定图可见；Edit 参考图槽扩到 3；AI 更新提示词逐镜（含运镜帧）；主体闸门 `cast`；cfg 真下发；官方出图档位（steps 40 / cfg 4.0 / shift 3.1） |
| `f77e229` | 「把所选参考图**物化**成真正的定妆照」资产（kind=portrait 复制件） |
| `7913aa8`（P13b） | ①定妆图生成也弹**正/负向提示词**（`GET …/revisions/{rid}/portrait-prompt` 预填，`CreateJobRequest.positivePrompt/negativePrompt`）；②「**+ 新增主体**」（`patchSubjectMeta` 支持创建、别称命中不覆盖别名）；③位置总控下拉新增「**定妆照·某主体**」+ 预览框渲染定妆照图片（拖图=拖位置）+ 切镜为空自动均分（改过则保留）；④参考图语义回归「只作定妆依据」（文案/tooltip 全改）；⑤第4镜错位/张冠李戴真因修正（`Picture N` 并列、位置覆盖句、**禁止 LLM 写画面方位**、参考图上传失败即中止、worker 追加语跟随语言） |
| `955b7c2`（P14） | ①`plan.setting.{era,notes}` 设定年代（导演必须输出；追加进每镜正词）；②主体档案 `gender/age/height/build/personality/appearance`（抽取+手填）；③三处同口径注入 + 定妆照提示词带档案与「**严重禁止画成女性/男性**」；④`mergePrevSubjectsAndSetting` 保护用户主体数据不被 LLM 覆盖 |
| `a97a772`（fix） | 位置总控「作用范围」**可搜索下拉线上不显示** —— `ProjectDetailView.vue` 用了 `<NSelect>` 却漏 `import`（Vue 只 warn，渲染成空元素；`vue-tsc`/`vite build` 都不报）；已修 + 新增 `npm run check:components` 挂在发版前 |
| `7f7e939`（数据） | 待办 3：三个生产方案补齐 `setting.era` + 人物档案（脚本 `deploy/backfill_setting_traits.*`，走产品接口、只补空、幂等）；`--print-portrait` 免 GPU 验证定妆提示词 |
| `063bbc5`（fix） | **保存路径也保护 `setting`** —— 实测 rev52（有 setting）被“P14 之前就打开的旧页面”连存 rev53/54/55 逐步洗掉，era 整键消失 → 那批任务正词里没年代。新增 `DirectorService.inheritSettingIfAbsent()`：**逐字段补空**（提交里没这个键 / era 是空串 且上一版有值 → 继承；写了非空值以提交为准；两边都空不留空对象）；挂 `patchRevision` + `plan/inplace`；+4 单测（180 全绿）。**已部署（21:52）**，并用 `--selftest-guard` 在一次性项目上验两种旧草稿形态 → PASS |
| `4719266`（ref） | 参考图只留「勾选/删除」两态（从资产点参考＝**复制一份** kind=reference）+ 定妆照**零参考直接拒**（实测出噪声图：脸探针命中 0、块内 std 0.191 vs 0.017） |
| `4c00f5a`/`1d44594`（UI+文档） | 位置总控加 `脸≈NNpx` 预告（<64px 红 / 64~96px 黄）；铁律与阈值写入 `Weaveora.md §7.5.1`（含 md5 硬证据：区域坐标**从不**裁/缩图） |
| `108f91f` 之后（本次 23:4x） | ① 生成**前**“脸过小”确认闸门（不做事后告警 —— 用户裁定：事后告警白烧 GPU）；② motion 帧数自动算（镜头时长×16fps）+ 实时显时长 + 不足红字；③ **修 motion 降帧误判**（判据改用实测可用显存；worker 已部署） |

**验证到什么程度**

- `cd api && mvn -o test` → **176 例全绿**（`JobLayoutRegionsTest` 18、`PlanSubjectsTraitsTest` 7、`SubjectPromptsTest` 6、`DirectorMergePrevTest` 11）。
- `cd web && npm run type-check && npm run build:prod` 通过。
- 线上 API 实测（一次性项目 + 临时 token，已清理）：新增主体、写入/读回档案、`portrait-prompt` 返回 traits 且含性别禁止句、**按别称也能查到同一份档案**。
- ❌ **未验证**：GPU 出图（服务不可用）。所以"宝玉是否真的不再被画成女性 / 位置是否不再错位"**尚未闭环**。

---

## 8. 待办（按优先级）

> 🔔 **等用户确认 / 要主动提醒的项**（用户明确说过“以后提示”，新会话请先在对话里提这两条）：
> 1. **txt2img 通路的引擎参数 A/B**（§8 待办 2 里的 ⏳ 项）：零参考入口已封，但纯文生图**能否出好图从未验证**；
>    计划用 ComfyUI `/prompt` 直提两发（同种子同提示词）：A=引擎参数 `steps 40 / cfg 4.0`（当前生效） vs B=工作流电影档默认 `steps 24 / cfg 3.5`，
>    比对人脸命中 / 块内 std。需在 GPU 空闲时做（做完要征得用户确认才改引擎参数）。
> 2. ⛔ **用户已裁定「暂不处理」（09-17）** —— 主体闸门是否改成“按帧逐个检查”（2026-09-17 用户原话：“遗留，以后提示”）：
>    现状：闸门只按**镜级**文本判断；帧级 `cast` 未写时会继承镜级（所以不会出现“随机出图”），
>    但“关键帧换了人、而镜文本没点名”的镜不会被额外提示。做成按帧检查会更严，代价是多几次确认框 → 需用户拍板。
> 3. ✅ **已做（09-17 13:0x）**：用户点头 → 本机 `git push origin master` **一次推全**（剧本 6 + 伙伴 4 + 本机 2）。
>    口径：**只允许一端推**，另一端 `git pull`；那个 0 字节未跟踪文件 `可用` 没带进提交。
> 4. ✅ **已止血（09-17 12:30）**：GPU 地址指向**非我方实例**（详见 §1 的 GPU#2 行）→ 停 `weaveora-gpu-worker` + 生成前置闸门（503，提交即报错不再静默排队）。
>    **用户裁定「GPU 暂时不管」**；等平台给回真地址/端口/SSH 授权后：伙伴改 `services.*.url` 并起 worker，我做只读探活 + 回归核对，再跑那 5 条 clip 的重试。

1. ✅ **已做（2026-09-16 21:26）**：SSH 新端口 `31012` 已验证可用，本机 `~/.ssh/config` 三个别名（`gpu`/`gpu2`/`weaveora-gpu-a14b`）全部改成 31012 并逐个连通测试；网关/ComfyUI 内外网 200；机器重开后服务**自动起来**（8001/8091/8093/8094/8800，无需手工 boot）。
2. **出图验证（最重要）**：重跑**第 4 镜（3 主体）** + **一镜宝玉单人镜**，核对 ①性别 ②位置 ③年代元素 ④面部一致性；若仍串脸 → 做单口径 `Picture N` A/B（见 §8 追加项）。
   （注：21:24 起用户已自己在跑 3 个 still 任务，观察它们的结果即可，不必重复跑）
   ➜ ⏳ **待验证项（等 GPU 空闲；★ 用户说“等他验证后提醒他确认”）**：**txt2img 通路的引擎参数是否把画面打爆**。
      背景：林黛玉 22:19 那张定妆照是**零参考** → 回退到纯文生图 → 出噪声图（人脸探针命中 0；块内 std 0.191 vs 正常 0.017；文 1.9MB vs 0.75MB）。
      零参考入口已封（后端拒 + 前端禁用按钮），但 **txt2img 通路能否出好图从未验证过**（本机历史成功出图**全是 edit**）。
      要做：ComfyUI `/prompt` 直提两发（同种子同提示词）—— A：引擎参数 `steps 40 / cfg 4.0`（当前生效）｜B：工作流电影档默认 `steps 24 / cfg 3.5`；
      比较两图的人脸命中 / 块内 std。若 A 坏 B 好 → 给 txt2img 单独一套参数（引擎配置只存一份 steps/cfg，两种通路共用是隐患）；
      若两发都坏 → txt2img 通路本身有问题（对照官方模板：cfg 2.5 / steps 20 / shift 3.0）。
   ➜ **2026-09-16 22:00 实测第 4 镜（rev53）：参考图确实发出去了（Picture 3 = 警幻定妆照，三处对得上），但身份只对宝玉生效** —— 探针结果：出图只检出 1 张脸（100px / 占画面 1.3%），与宝玉定妆照相似度 0.609（同一人），与警幻/可卿只有 0.24/0.19（检不到）。真因 = **位置框是全身比例**（自动分配默认 w33~50 / h=100；该镜 0.19×0.76）→ 脸太小 → 身份约束失效。工具：`deploy/diag_face_refs.py`（人脸探针，可把“发没发出去”与“发出去但尺度太小”分开）。
3. ✅ **已做（2026-09-16 夜）**：三个有主体的生产方案补齐 `setting.era` + 人物档案 —— `deploy/backfill_setting_traits.py`
   走产品接口写（`PATCH …/plan/inplace` 写年代、`POST …/subjects/meta` 写档案），**只补空字段、幂等**（跑第二遍输出 `无变化`）。
   覆盖：`那宝玉恍恍惚惚`（rev52，含当晚新加的林黛玉）、`游幻境指迷十二钗…`（rev12）、`关羽追杀吕布`（rev1）。
   免 GPU 验证：`--print-portrait 宝玉,可卿` 让后端用**新写入的档案**渲染定妆照提示词（可见「性别 男 male … 严重禁止画成女性」）。
   新方案不用回填：导演生成必须给 `setting.era`；档案点「一键生成主体」自动补（只补空）或「主体设定」手填。
4. **前端人工点检**：定妆弹框 / 新增主体 / 位置总控（选「定妆照」+ 拖图片 + 切镜自动均分）/ 参考图文案 / 主体设定弹框 + 未填性别红标 / 方案页「设定年代」。
5. 可选（需产品点头）：未填 era / 未填性别时**硬拦确认**（现在是红字提醒 + 二次确认，不阻断）。
6. 可选（出图质量）：**把主体框改成半身比例**（自动分配默认 `w33~50 / h100` 是全身 —— 脸只占画面 1.3%，身份锚定几乎失效；半身 `h≈0.45~0.6` 能脸大 2~3 倍）；`FluxKontextMultiReferenceLatentMethod("index_timestep_zero")` A/B；mask + Inpaint Crop & Stitch；3 主体镜拆成 ≤2 主体；画幅改 1568×896（让参考图尺寸是 112 倍数）。
7. 参考图语义若要从"只作定妆依据"再进一步（**彻底去掉**参考图的加入/勾选交互），需产品确认。

---

## 9. 纪律与已踩的坑（浓缩，别重复付学费）

1. **重启 GPU 服务必须走 `bash deploy/gpu2_restart.sh`**（先查库：有 queued/running 就拦，`FORCE=1` 才强推）；**按 PID 精确杀**，`pkill -f` 会自匹配杀掉自己的 ssh（用 `[x]` 防匹配）。
2. **绝不在有任务时重启** API/worker/GPU 服务（曾把用户任务撞死，报 `502 Cannot connect to host 127.0.0.1:8001`）。
3. **不要静默降级/静默丢东西**：参考图失败要失败或明确告警；参考图多于槽位要告警；少槽位要摘空槽（不能"复用第一张"）。
4. **不要让 LLM 拥有用户数据**：`subjects[]`（定妆照/素材图/别名/档案）只能由用户与产品逻辑写；LLM 产出必须丢弃。
5. **不要两套方位并存**：位置只由用户在「位置总控」设定，并由系统作为最终裁定写进正词；LLM 文案禁写 left/right/foreground。
6. **中文人名的性别不是常识**：任何要画人的地方都必须显式给 `gender`（拿不准写 other，不能猜反）。
7. **排障顺序**：先看 `/queue`（僵尸 prompt）→ worker 日志 → ComfyUI 日志（过滤 `non matching host` 噪声）。
8. **交付一律给可点开链接**（`web/_share/` → `https://sysou.com/weaveora/_share/<file>`）。
9. 大文件下载走 `deploy/windows/gpu_model_downloader.js`（10 路 Range + 断点），禁止 `huggingface_hub` / 裸 curl（详见 `Weaveora.md §0.2`）。
10. **用户页面比数据库“旧”时，整份回写会静默丢键**（2026-09-16 实测：新增的顶层键 `setting` 被旧草稿连存三版逐步洗掉，era 没了）。规矩：新增顶层用户字段时，**保存路径也要写“空白提交就从上一版补空（逐字段）”**（后端 `DirectorService.inheritSettingIfAbsent` 已做、已部署）；代价是该字段不能再靠“清空”删掉（era 本来就是必填语义）。复核命令：`python3 deploy/backfill_setting_traits.py --data deploy/backfill_setting_traits.json --selftest-guard <一次性项目> --dry-run` 不需要，直接跑带 `--selftest-guard`。
11. **先用数据验证再动 GPU**：拿到任务后先看 `payload->>'positive_prompt'` 里有没有该有的块（年代/`Picture N = 主体[性别…]`），比出完图再猜快得多（本次就是这么早发现“年代没进提示词”的）。

---

## 10. 常用命令速查

```bash
# 本地
cd api && mvn -o test                 # 单测（176 例）
cd web && npm run check:components    # 前端自检：模板用了但没 import 的组件（build 已内置）

# 部署后自检（VPS 上）：验证「保存路径的 setting 守卫」还在生效（用一次性项目，自清理）
ssh sysou 'cd /tmp/wv-bf && python3 backfill_setting_traits.py --data backfill_setting_traits.json --selftest-guard unlock-test'
cd web && npm run type-check          # 前端类型检查
cd web && npm run build:prod          # 前端产物（base=/weaveora/）

# VPS（root@sysou.com）
systemctl is-active weaveora-api weaveora-cloud-worker weaveora-gpu-worker
journalctl -u weaveora-gpu-worker -n 80 --no-pager      # 出图/出片 worker 日志
sudo -u postgres psql -d weaveora -c "select state,count(*) from generation_jobs where state in ('queued','running') group by state;"
sudo -u postgres psql -d weaveora -At -c "select jsonb_pretty(services) from user_engine_settings order by updated_at desc limit 1"

# 看某次任务真实的提示词（排查提示词问题最快的一条路）
sudo -u postgres psql -d weaveora -At -c "select jsonb_pretty(jsonb_build_object(
  'shot_no',payload->'shot_no','subjects',payload->'referenceSubjects','regions',payload->'referenceRegions',
  'positive',payload->'positive_prompt','negative',payload->'negative_prompt'))
  from generation_jobs where id='<jobId>'"
```

---

## 11. 研究资料索引（均已入库，无需再去 .scratch）

- **入口：`docs/知识档案索引.md`** —— 按场景告诉你该读哪份（新会话可根据它挑文件，不必全读）。
- `docs/research/qwen-image-edit-official.md`（~36KB，**一手材料**）：官方仓库/HF 模型卡、ComfyUI 官方模板 JSON 逐节点解析、ComfyUI 核心源码（含 `Picture N` 口径、参考图内部缩放、CFG/shift 官方值）。**改提示词/出图参数先读它**。
- `docs/research/qwen-image-edit-community.md`（~43KB，二手）：社区实测技巧（多主体同框成功率排序、负词无效、112 倍数尺寸、Crop&Stitch…）；文首标了抓取限制与降权来源，引用前先看那段。
- `docs/notes/运维铁律-GPU与生产.md`、`docs/notes/出图管线-经验与坑.md`、`docs/notes/对口型-经验与坑.md`、`docs/notes/本机工具链坑.md`、`docs/notes/环境与纪律-总纲.md`
  —— 历次事故复盘迁入仓库（原在 `.scratch/`，已不在那儿）；**涉及 GPU 地址/端口的以平台配置页为准**。
- GPU 服务器搭建/模型清单：`GPU服务器能力搭建指南.md`、`docs/gpu-newserver-models.md`、`docs/gpu-newserver-batch1.md`、`docs/wan22-dual-expert-prep.md`、`docs/lipsync-setup.md`。
- 产品/架构唯一真源：`Weaveora.md`（其中 **§7.5.1 定妆照与参考图**、**§7.5.2 设定年代与主体档案** 是本轮新增，改这三件事前先读）。
- `.scratch/` 现在只剩部署日志（`.gitignore` 排除），**不需要再读**。
