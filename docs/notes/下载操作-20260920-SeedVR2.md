# 下载操作记录 · 2026-09-20 · SeedVR2（放大 / 细节修复权重）

> 起因：用户指令「执行第 3 条」——为「关键帧人脸不清晰」补一条**放大 + 细节修复**收尾（1MP 出图 → 放大交付）。
> 性质：**只新增文件**，无删除 / 移动 / 覆盖。
> 依据：`GPU服务器能力搭建指南.md` §2.9「放大 = 无」→ 本次补齐；来源级别 = ComfyUI 官方组织在 ModelScope 的 repack（①②级来源）。

## 1. 新增对象（字节数下载前后与 `Content-Range` 逐一核对）

| 文件 | 字节数（下载后 `stat`） | 落盘位置 | 盘 |
|---|---|---|---|
| `seedvr2_3b_fp8_e4m3fn.safetensors` | **3,392,794,232** | `/addDisk/weaveora/models/diffusion_models/`（真文件）<br>+ `/opt/weaveora/models/diffusion_models/` 软链（与 `qwen_image_edit_2511_fp8mixed` 同规） | `/addDisk` |
| `ema_vae_fp16.safetensors` | **501,324,814** | `/opt/weaveora/models/vae/` | `/` |

**下载地址（ModelScope，HTTPS Range 实测 206 + `content-range` 与上表字节数一致）**

```
https://modelscope.cn/models/Comfy-Org/SeedVR2/resolve/master/diffusion_models/seedvr2_3b_fp8_e4m3fn.safetensors
https://modelscope.cn/models/Comfy-Org/SeedVR2/resolve/master/vae/ema_vae_fp16.safetensors
```

## 2. 执行方式（§0.2 大文件铁律：多路并行 + 断点续传 + 后台静默）

- 工具：盒上既有 `/opt/weaveora/gpu_mp_download.sh <URL> <OUT> [并发=10]`（10 路 Range 分片、片内断点续传、每片字节校验、写 `.done`）
- 编排脚本：`/opt/weaveora/dl_seedvr2_20260920.sh`（`export WEAVEORA_CURL=curl`，铁律⑦）
- 日志：`/opt/weaveora/logs/dl_seedvr2_20260920.log`
- 实测：**22:38:14 → 22:42:59（4 分 45 秒）**，速率 ≈16 MB/s，`fail=0`，两文件均写 `.done`

## 3. 磁盘前后

| 挂载 | 下载前 | 下载后 |
|---|---|---|
| `/`（vda1） | 18 G 可用（91%） | **17.5 G** |
| `/addDisk`（vdb1） | 55 G 可用（42%） | **51.8 G** |

## 4. 验收（ComfyUI 已认到）

1. `GET /object_info/UNETLoader` → `unet_name` 列表含 `seedvr2_3b_fp8_e4m3fn.safetensors` ✅
2. `GET /object_info/VAELoader` → `vae_name` 列表含 `ema_vae_fp16.safetensors` ✅
3. 原生节点在位（ComfyUI 0.34.0 自带 `comfy_extras/nodes_seedvr.py`）：`SeedVR2Conditioning` / `SeedVR2Preprocess` / `SeedVR2PostProcessing` / `SeedVR2TemporalChunk` / `SeedVR2TemporalMerge` ✅
4. 节点签名（实测 `/object_info`）：
   - `SeedVR2Conditioning(model: MODEL, vae_conditioning: LATENT) → CONDITIONING ×2`
   - `SeedVR2Preprocess(resized_images: IMAGE) → IMAGE`
   - `SeedVR2PostProcessing(images: IMAGE, original_resized_images: IMAGE, color_correction_method) → IMAGE`

## 5. 接线口径（待搭工作流；Comfy-Org `workflow_templates` 里**没有** SeedVR2 模板，需自搭）

```
LoadImage → SeedVR2Preprocess(resized_images)
  → VAEEncode(pixels, vae=VAELoader(ema_vae_fp16))
  → SeedVR2Conditioning(model=UNETLoader(seedvr2_3b_fp8_e4m3fn), vae_conditioning=该 latent) → positive/negative
  → KSampler → VAEDecode(vae=ema_vae_fp16)
  → SeedVR2PostProcessing(images=解码结果, original_resized_images=Preprocess 输出)
  → SaveImage
```

## 6. 回滚（属删除操作，须先列清单并经用户逐项确认，本次不预授权）

```bash
# 仅列出待删对象，不执行
ls -l /addDisk/weaveora/models/diffusion_models/seedvr2_3b_fp8_e4m3fn.safetensors*
ls -l /opt/weaveora/models/vae/ema_vae_fp16.safetensors*
ls -l /opt/weaveora/models/diffusion_models/seedvr2_3b_fp8_e4m3fn.safetensors
```
