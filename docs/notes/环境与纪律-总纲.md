# 环境与纪律总纲（记忆浓缩版）

> 来源：本机记忆浓缩 `memory-condensed.md`（2026-09-16 迁入仓库）。
> ⚠️ **已脱敏**：测试账号口令与开发库密码不入库。文中路径/表名/开关名可直接引用。

【Weaveora 总纲：环境/部署/分支/生产/功能/位置/前端/云API/GPU】
环境：dev=本机 PG18.4 weaveora_dev(localhost:5432 postgres/<本机开发库密码>)；prod=weaveora、test=weaveora_test；Redis db0=prod/db1=test(复用 MirrorTalk VPS sysou.com，勿动 mirrortalk；凭据 ~/.weaveora/vps-mw.env；生产 PG 凭据在 VPS mirrortalk.service 勿入库)。Node22+npm(npmmirror，无 pnpm)；api=Java21+Boot3.4.5+Modulith1.3+JPA/Flyway/Security/Redis；deploy/web/worker。测试账号 tester@weaveora.studio（密码见本地记忆，勿入库）。
部署：JAVA_HOME=jdk-21.0.12-hotspot；Redis 隧道 6379；cd api && mvn -q spring-boot:run(WEAVEORA_JWT_SECRET,8080)；web=/opt/weaveora/web/；jar=/opt/weaveora/api/weaveora-api.jar(systemd weaveora-api + weaveora-cloud-worker)。上线=package+build:prod→tgz marker 校验→scp→备份→替换→restart→journalctl 证据；deploy/api-deploy.sh 五步、有 running 任务拒重启(WEAVEORA_API_FORCE=1)、回滚 .bak.<ts>。改仓库≠上线须明说『已部署/未部署』；部署前 diff 现网 vs HEAD，部署后 unzip -l jar|grep ClassName 验线上。
分支纪律(勿违)：真开发线=origin/master(另一机 push，领先 main 25+)；本机 main 陈旧勿覆盖生产；改前 git fetch origin+git branch -a；push 一律 master；main 修复 cherry-pick 回 master(JobService/ProjectDetailView 常冲突)；只提交本次 hunk、先 diff 识别人 WIP、勿夹带 vram_stats/motion_plan、勿入库 .deploy。
生产纪律：长 LLM 勿同步 HTTP(nginx proxy_read_timeout 1800s，后台+轮询)；RestClient/worker 须显式 .body(body) 否则 DeepSeek 400；secrets 走 EnvironmentFile(/etc/weaveora/weaveora-api.env 0600)；删 job 级联删资产行+文件；SSH 隧道僵死→杀旧 PID 重启；媒体 /opt/weaveora/data/storage/<ws>/<project>/<job_id>/<uuid>。
功能/数据：自动成片+ffmpeg 轻精修、默认 300s(WEAVEORA_VIDEO_MAX_DURATION_SEC)+分段导演(>60s 分 K 段、shot_no 全局重编号)；QuotaService Redis 配额 director20/still200/clip600s 日切零点 fail-open；集市分享需 admin sysou.com@outlook.com 审批；非白名单建项目 403(WEAVEORA_ACCESS_RESTRICT_CREATE=true+CREATOR_ALLOW_SUFFIX=@weaveora.dev)。『只看最近一轮』按每分镜每类(still/clip) 最新 createdAt max；referenceAssets=[{assetId,subject}] 按 subject 匹配、无 subject 始终锚定；keyframes(2–4帧)、assets.shot_no(V9，读侧优先 (project_id,shot_no,kind))、云通道强 IPv4+瞬断重试。
位置/提示词：优先级 keyframes[].layout(帧级最高)>shots[].layout{subject,x,y,w,h}>方案级 referenceAssets[].region/subjects[].refs[].region>shots[].lipsync_targets(点选，默认框 0.30×0.45)；归一化 0–1，w/h 表远近；后端把「image1=谁/image2=谁」+位置句写进 positive_prompt(不靠 LLM，中文配中文)；Edit 槽 image1..3(节点12/14/16)。UI 每镜「AI 更新提示词」(无全局 aiSyncAll，会卡死)，中/英可选，运镜覆盖起止两帧；某镜未指明主体→必须弹框勾选。schema_json=jsonb 用 utils/plan.ts canonicalJson()(勿 JSON.stringify)；『改方案但生成没变』查 generation_jobs.payload(revision_no/prompt_md5/referenceKeys)+approved_revision_id↔assets/shot_drafts；『保存并确认』即设基准，仅关键帧/motion 前强制确认。`app_settings` 表不存在(engine 用 EngineSettings)。单测 RefSubjectMatchTest(中文名须容忍省姓氏，否则「秦可卿」静默丢参考图)、JobLayoutRegionsTest。
前端交互：版本号用 NSelect filterable 下拉、默认最新版；「按配音校准时长」归位「镜头时长」区；任务区按钮按流程 关键帧→运动→配音→对口型→配乐 且 title 标第几步。顶部「位置预览」=作用范围可选的位置总控(方案默认/第N镜/第N镜·第N帧)，分镜内「画面位置（本镜）」=逐镜快捷写 shots[].layout；x/y/w/h 0–100% 可输入(越界回推不裁尺寸)。
云API：测试只用 replicate.com(图 stability-ai/stable-diffusion:ac732df8… 尺寸 64 倍数；视频 prunaai/p-video ≤720p)，须显式 WEAVEORA_REPLICATE_TEST_MODELS=1；生产以用户 Engine Settings 为准，不得静默替换，未配则报错引导去设置。
GPU：⚠️ 公网 IP/端口禁止写死进代码/配置/脚本/文档；唯一入口=「生成引擎配置」页 EngineSettings.services(tts/music/lipsync/face/transcribe/talk/image)，随任务下发即刻生效；新增能力须补 EngineSettingsView。ComfyUI=<gpu>:8001、配音/转写=<gpu>/audio、整脸口型=<gpu>/talk、人脸=<gpu>、配乐兜底=<gpu>:8092。GPU#1=OneThingAI(端口/密码轮换用前索取；Comfy :7860；VPS 隧道→GPU:8188；重启须平台授权勿自行重试；就绪前用 stub)。坏镜=第5镜(勿写第6，引用前回生产库按 shot_no 核验)；拒绝文案须标明「该镜」原因+可执行建议。
GPU#2=weaveora-gpu-a14b：Ubuntu24/RTX4090 48G；ssh weaveora-gpu-a14b(+…-mobile，IP/端口轮换；**2026-09-16 夜实测 -p 31012**，key=~/.ssh/gpu_server_ed25519，本机别名 gpu/gpu2/weaveora-gpu-a14b 已同步)；登录后必 hostname/df 确认真机；/opt/weaveora=工作根，大模型软链进 models(挂载点会变，重建软链即复活)；端口 8001 ComfyUI/8091 TTS/8092 配乐/8093 face/8094 talk(EchoMimicV3)/8800 edge_proxy。⛔ 禁 pkill 杀 comfy 主进程；face/talk 无 systemd→services_up.sh 必含启动块且幂等。底片体检=出片前硬门禁(env WEAVEORA_LIPSYNC_*)；shots[].lipsync_source='still'|'clip' 逐镜选。模型：talk=EchoMimicV3、图=Qwen-Image/Edit fp8、Wan2.2 I2V-A14B+lightx2v 4step、T2=FLUX.1-schnell；节点补丁部署前必 diff(《GPU服务器能力搭建指南.md》)。调度/预热：JobService.claim() 优先挑同 kind；deploy/gpu2_warmup.sh 挂 services_up.sh。资源/重启/维护细节见 failure 记忆。
出图参数/画幅：Edit shift3.1/steps40/cfg4.0(cfg 过低=无电影感)、denoise=1.0；ASPECT_DIMS 16:9=1280×704、9:16=704×1280、1:1=1024²、3:2=1152×768、2:3=768×1152。JSON 仅默认：worker env WEAVEORA_IMAGE_STEPS/CFG 与 EngineSettings.services.image.{steps,cfg,denoise,workflow,editWorkflow} 会覆盖；改 JSON 须同步 DB user_engine_settings.services.image 与线上 /opt/weaveora/workflows/；真实生效看 worker 日志 `RESULT IMAGE_CFG=? steps=? shift=?`，勿只看 UI/JSON。
