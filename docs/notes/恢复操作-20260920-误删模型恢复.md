# 恢复操作清单 · 2026-09-20（误删模型 #6 / #7）

> 起因：2026-09-19 助手未经逐项确认删除 7 个模型（79.5 GB）。
> 本次**只恢复用户指定的 2 个**（#6 / #7），其余 5 个（#1–#5）**不动**。
> 本次操作性质 = **新增文件**（两个目标路径经核实**不存在任何实体文件/分片/.done**，不涉及删除 / 移动 / 覆盖）。
> 依据文档：`docs/待恢复模型清单-2026-09-19.md`、`GPU服务器能力搭建指南.md` §2.1、`docs/gpu-模型清单与镜像备份.md`。

## 1. 操作对象

| # | 目标绝对路径（GPU 盒） | 期望字节数 | 期望 sha256 | 磁盘 |
|---|---|---|---|---|
| 6 | `/opt/weaveora/models/diffusion_models/qwen_image_fp8_e4m3fn.safetensors` | 20,430,635,136 | `98763a127701eb6fb59096f7742cb3aa7d64ed510b9f4e882d8351f8176e3ce3` | 系统盘 `/`（真文件） |
| 7 | `/opt/weaveora/models/loras/Qwen-Image-Lightning-8steps-V1.0.safetensors` | 1,698,951,104 | `07b5a999881437f63124979844ba1949ce2438f65b6220628a196a7d30a4fff9` | 系统盘 `/`（真文件） |

`/opt/weaveora/ComfyUI/models` 是软链 → `/opt/weaveora/models`，故写入上表路径即等于 ComfyUI 可见路径（无需再加软链）。

## 2. 恢复地址（2026-09-20 00:2x 在盒子上实测 `HTTP 206` + `Content-Range` 精确字节数，与上表一致）

```
#6  ModelScope  Comfy-Org/Qwen-Image_ComfyUI  split_files/diffusion_models/qwen_image_fp8_e4m3fn.safetensors
    https://modelscope.cn/models/Comfy-Org/Qwen-Image_ComfyUI/resolve/master/split_files/diffusion_models/qwen_image_fp8_e4m3fn.safetensors
    → content-range: bytes 0-0/20430635136   ✔ 与期望一致

#7  ModelScope  lightx2v/Qwen-Image-Lightning  （上游作者组织仓库）
    https://modelscope.cn/models/lightx2v/Qwen-Image-Lightning/resolve/master/Qwen-Image-Lightning-8steps-V1.0.safetensors
    → content-range: bytes 0-0/1698951104    ✔ 与期望一致
```

来源级别：`Comfy-Org/*` 是 ComfyUI 官方组织在 ModelScope 的 repack 仓库（ComfyUI 官方文档指定来源）；
`lightx2v/*` 为蒸馏 LoRA 的**上游作者组织**官方仓库。二者均为 ①②级来源，非第三方二手页。
（HF 在盒子上不通 → 只走 ModelScope，见指南 §4.1-1。）

## 3. 执行方式（§0.2 大文件铁律：多路并行 + 断点续传 + 后台静默）

- 工具：`/opt/weaveora/gpu_mp_download.sh <URL> <OUT> <并发=10>`（盒上已有，4192 B，与仓库 `deploy/gpu_mp_download.sh` 同版本）
  - 10 路 Range 分片、**片内断点续传**、每片字节数校验、全片齐了才按序 append、写 `.done`。
- 后台化脚本：`/opt/weaveora/dl_restore_20260920.sh`（顺序执行 #6 → #7）
- 日志：`/opt/weaveora/logs/dl_restore_20260920.log`
- 中途产物：`/opt/weaveora/models/<dir>/.wvparts_<name>/pN`（重跑同一脚本自动续传）

## 4. 磁盘闸门

| 挂载 | Size | Avail | 需求 |
|---|---|---|---|
| `/`（vda1） | 196 G | **39 G** | 20.6 G ✔ |
| `/addDisk`（vdb1） | 98 G | 55 G | 本次不用 |

> ⚠️ `/addDisk` 上还躺着融合版 `qwen_image_edit_2511_..._4steps_v1.0.safetensors`（19.04 GB，**非误删项**）——**本次不动**。

## 5. 验收

1. 两文件字节数 = 上表；`<file>.done` 存在；
2. `sha256sum` = 上表；
3. `curl -s http://127.0.0.1:8001/object_info/UNETLoader` 能看到 `qwen_image_fp8_e4m3fn.safetensors`；`/object_info/LoraLoaderModelOnly` 能看到 `Qwen-Image-Lightning-8steps-V1.0.safetensors`；
4. 可选：零参考空镜出图 / `first_image.py` 暖机（由主线会话做）。

## 5.1 执行结果（2026-09-20 00:47 实测，三项验收全过 ✅）

| # | 落盘字节数 | 期望 | sha256 实测 | 期望 | `.done` | safetensors 头 |
|---|---|---|---|---|---|---|
| 6 | 20,430,635,136 | 20,430,635,136 ✔ | `98763a127701eb6fb59096f7742cb3aa7d64ed510b9f4e882d8351f8176e3ce3` | 同 ✔ | 有 | header 234,040 B / tensors **1933** / metadata **无** |
| 7 | 1,698,951,104 | 1,698,951,104 ✔ | `07b5a999881437f63124979844ba1949ce2438f65b6220628a196a7d30a4fff9` | 同 ✔ | 有 | header 252,216 B / tensors **2160** / metadata 无 |

- 时间线：`00:20:58` 启动 → `00:44:5x` #6 完成（含分片拼接）→ `00:46:58` #7 完成 → `00:47:35` sha256 完成。全程 **~27 分钟**；`gpu_mp_download.sh` 均速 ≈ 14–15 MB/s（10 路聚合）。
- **tensors=1933 + 无 metadata** 与 `CLAUDE.md` 记录的「base 档指纹」一致（fp8mixed 是 tensors≈4451 + 带 `_quantization_metadata`）⇒ 排除下错成 `fp8mixed` 的可能。
- ComfyUI 可见性（`curl 127.0.0.1:8001/object_info/…`，**未重启** ComfyUI，靠逐请求重扫目录）：
  - `UNETLoader.unet_name` → `…qwen_image_edit_2511_…4steps_v1.0`、`…qwen_image_edit_2511_fp8mixed`、**`qwen_image_fp8_e4m3fn.safetensors` ✔**
  - `LoraLoaderModelOnly.lora_name` → `…2511-Lightning-4steps-V1.0-bf16`、**`Qwen-Image-Lightning-8steps-V1.0.safetensors` ✔**
- 日志：`/opt/weaveora/logs/dl_restore_20260920.log`（`DONE exit6=0 exit7=0`）、`/opt/weaveora/logs/sha_restore_20260920.txt`。
- 旁证：下载期间 GPU 服务未重启、`/queue` 未干预；恢复后系统盘 `/` 余 **18 G**（91%）。

> 未做（交接主线会话）：跑 `first_image.py` 暖机或出一张**零参考空镜关键帧**，验证 #6/#7 的用户面（那是它们被删时的直接受损通路）。

## 6. 回滚 / 风险

- 本操作只**新增**文件，失败态 = 半截文件 + `.wvparts_*` 分片目录（可重跑续传）。
- 若需撤销：删除上述 2 个文件 + `.done` + `.wvparts_*` —— **属删除操作，须先列清单并经用户逐项确认**（本次不预授权）。
- 风险：系统盘 `/` 余 39 G，下 20.6 G 后余 ~18 G（ComfyUI 出图仍可用，但系统盘偏紧 → 后续可考虑迁移到 `/addDisk` + 软链）。
