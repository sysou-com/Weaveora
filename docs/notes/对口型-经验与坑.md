# 对口型经验与坑（LatentSync / EchoMimicV3 / 底片体检）

> 来源：本机记忆沉淀 `consol_lipsync.txt`（2026-09-16 迁入仓库）。
> 背景详见 `可行性分析-对口型-极端表情-2026-09.md`。

[insight] Weaveora 对口型（v2v 掩码替换/底片/门禁）：LatentSync/MuseTalk/Wav2Lip/KeySync 均「嘴部掩码替换」v2v（只重画嘴周、不管下颌开合）：底片嘴已大张→掩码与掉下颌几何冲突=画面坏（mouth_open 1.242）。大喊/惊吼镜「底片二选一」死结（用户不接受），正解=jaw-lip 解耦或整脸生成 EchoMimicV3（GPU#2 kind=talk :8094，部署见运维铁律⑮；底片=该镜静帧+配音 t2v；已接线须按验收证据切换勿一步替换）。此类镜先出方案评审（认可语：全收/要/2/A,B,C）勿直接调参；见 可行性分析-对口型-极端表情-2026-09.md（§4/§9）commit 353823c/786a57f。坏镜实证=《那宝玉恍恍惚惚》第5镜（非第6镜，用户裁定），底片 mouth_open 1.232/1.238；引用镜号前必回生产库按 shot_no+action/正词核验。修法=重出「嘴巴自然闭合近景静帧」当底片（正词 close-up+lips pressed shut/jaw clenched，负词 open mouth/parted lips/visible teeth），惊叫/大喊自动分流静帧底片。底片选择：shots[].lipsync_source='still'|'clip' 逐镜指定，不写=自动；指定 still 时后端取「该镜最新 still 资产」→易被后续测试出图顶掉（用户看到没参考 motion、脸凭空生成）；'clip' 走片段路径（已加片段首帧底片支持）；排查先查 payload 的 videoKey/isStill/lipsyncSource 与 asset 时间戳。体检阈值（worker 出片前可配勿回落）：mouth_idx=52–71（WEAVEORA_LIPSYNC_MOUTH_IDX 可覆盖；误用 87–105 致 mouth_open 全 null 静默跳门禁）；MOUTH_MAX=1.05 拒/WARN=0.90（旧 0.30/0.50 误拦太重）；FACE_MIN_PX=64 拒/WARN=96，无人脸 ratio=0.015；嘴优先于脸；拿不到指标一律跳过绝不误拦；指标出自 deploy/face/face_server.py（:8093），worker 与服务端必须同更否则静默跳门禁。有「点选人脸」时不做识别式预检（insightface 对风格化古风不可信），点选即权威。依赖坑：LatentSync object_info inference_steps/fps/seed/lips_expression 注入必须 clamp 否则 POST /prompt 400；nodes.py 需 import torchvision.io as io；imageio v3 write_video 须 macro_block_size=None 且勿传 plugin=；venv 无 pip→ensurepip；setup_models() 无条件拉 HF→hosts 屏蔽+删 *.incomplete，权重软链 checkpoints/latentsync_unet.pt、whisper/tiny.pt，建 /root/.latentsync16_dependencies_installed flag；首实例化 pip install accelerate。