# 交接快照 · 2026-09-16（GPU 出图/出片链路）

> 用途：**开新会话时先读这个文件**，不要再去翻旧会话（旧会话已 14.5 MB，禁止继续堆）。
> 纪律：见 `Weaveora.md §0.3 上下文与输出纪律`（按任务开新会话 / 禁止倒原始日志 / 禁止轮询循环 / 大文本走 ctx / 长输出先落盘）。

## 1. 环境（会变，权威来源=平台「生成引擎配置 → GPU 服务器地址+端口」）

| 项 | 值 |
|---|---|
| GPU#2 ssh | `ssh root@180.127.11.167 -p 14812`（本机别名 `weaveora-gpu-a14b`） |
| 网关 | `http://180.127.11.167:14858` → 容器 8800（`/__edge/health` 可自检路由） |
| 服务 | ComfyUI:8001 · TTS:8091 · face:8093 · talk:8094 · 网关:8800（`weaveora-stack.service`） |
| 持久卷 | `/opt/weaveora`；数据盘 `/addDisk`（旧的 `/media/vipuser/addDisk` 是兼容软链） |
| VPS worker | `ssh root@sysou.com`，服务 `weaveora-gpu-worker`（`/opt/weaveora/stub_worker.py`），API `weaveora-api` |
| 交付目录 | VPS `/opt/weaveora/web/_share/` → `https://sysou.com/weaveora/_share/<file>` |

## 2. 模型（已升级，实测可用）

| 用途 | 文件 | 字节 | 备注 |
|---|---|---|---|
| 改图/关键帧 | `qwen_image_edit_2511_fp8mixed.safetensors` | 20,533,762,817 | sha256 `723d329b20c6cb7fee058ad626c988935f30b3f02057c7d7f679cdbe4dbe075d`；`fp8mixed` 必须配 `weight_dtype=default` |
| 文生图 | `qwen_image_2512_fp8_e4m3fn.safetensors` | 20,430,679,144 | sha256 `667a3ab94c95199add48c979bb597c9e196e747ba5444cdda952ec4354272a84` |
| 文本编码器/VAE | `qwen_2.5_vl_7b_fp8_scaled` / `qwen_image_vae` | — | 与旧版**同一个**，不用重下 |
| 出片 | `wan2.2_i2v_{high,low}_noise_14B_fp8_scaled` + lightx2v LoRA | — | A14B 双专家，见指南 |
| 对口型 | LatentSync 1.6（ComfyUI 节点） | — | 注意 B 保护（嘴大张会拒） |

- **ComfyUI 0.34.0 直接可跑 2511/2512**（实测加载+出图通过，无需升级/重启）。
- 模型放数据盘 + 软链进 `/opt/weaveora/models/diffusion_models/`（**软链漏了会报 `value_not_in_list` 400**，已踩过）。

## 3. 出图档位（当前配置，配置页可改，代码不写死）

```
services.image = {
  engine: comfy,
  workflow:         /opt/weaveora/workflows/qwen_image_txt2img_film_api.json   (2512)
  img2imgWorkflow:  ""            ← 不用 img2img（会把定妆照白底带过来）
  editWorkflow:     /opt/weaveora/workflows/qwen_image_edit_api.json          (2511, 官方结构)
  steps: 20, cfg: 2.5, denoise: 1.0
}
```
- Edit 工作流是**官方结构**：`TextEncodeQwenImageEditPlus`（prompt+参考图同时输入）+ **主参考图 `VAEEncode` 作采样起点**；**不要加 resize 节点**（节点内部会把参考图缩到 ~1MP）。
- **不装 Lightning LoRA**（用户要求：不为速度牺牲细节）。
- 实测：1280×704 / 20 步 ≈ 采样 4.4 s/步；缓存策略 `--cache-ram 32`（曾用 `--cache-none` 导致每张重载 20G+CPU fp8 转换，已修）。

## 4. 位置（路线 A 生效；两层位置 + 主体点名）

**两层位置**（后端 `JobService.applyLayoutRegions`，优先级从高到低）：

| 层 | 数据 | UI | 语义 |
|---|---|---|---|
| ① 逐镜 | `shots[].layout = [{subject,x,y,w,h}]` | 分镜卡展开 → 「画面位置（本镜）」（可拖拽 + w/h 缩放 + ±1% 微调） | **本镜**位置/远近（w/h 越大=越近） |
| ② 方案级 | `referenceAssets[].region` / `subjects[].refs[].region` | 方案页右卡「位置预览（相对位置 / 区域%）」 | **全片默认**位置（逐镜未覆盖时生效） |
| ③ 点选 | `shots[].lipsync_targets = {subject:{x,y}}` | 预览图点选人脸 | 只有点 → 默认框 0.30×0.45 |

- 归一化 0–1（相对画面宽高），`w/h` 表达**远近与大小**。
- 后端把它们写进 `referenceRegions`（与 `referenceKeys` 同序）**并拼进正词**：
  `Reference image mapping: image1 = 宝玉 (upper-left, x=0.19, y=0.09, box 0.30x0.45); image2 = 可卿 (...)`。
- **`image1` = `referenceKeys[0]`**（= `qwen_image_edit_api.json` 里 LoadImage 节点 `12`，节点按 id 字符串排序取图；`14` = `image2`）。改工作流勿打乱 LoadImage id 顺序。
- **槽位上限 = 3（2026-09-16 已扩）**：原工作流只接 2 个 LoadImage 槽 → **第 3 个主体不报错但图被静默丢掉**，这正是「第 4 镜（宝玉/可卿/警幻 三主体）一致性又崩」的真因：警幻完全没有参考图，而正词却写着 `image3 = 警幻`。现已加第 3 个 `LoadImage`（节点 `16`）并把两个 `TextEncodeQwenImageEditPlus` 的 `image3` 接上；worker 同时新增：**参考图多于槽位 → 明确告警**；**少于槽位 → 把空槽从图里摘掉**（不再「复用第一张」，避免同一张脸被注入两次）。某张参考图上传失败时槽位仍会前移一格（日志 `[comfy] 参考图#N 上传失败（跳过）`）。
- **即使没设任何位置，也照样写「imageN = 主体名」**（只点名、无坐标）—— 这是「positive_prompt 必须点名主体」的后端兜底，不依赖 LLM。无 subject 名的风格参考图不点名。
- **路线 B（区域条件）代码保留但默认关闭**：`WEAVEORA_IMAGE_AREA_COND=1` 启用；它在 Qwen-Image-Edit 的参考图 conditioning 上会让 KSampler 抛 `IndexError: tuple index out of range`（两轮实测），需离线找出成立写法（候选：挂在纯 CLIPTextEncode 基础 conditioning 上 / `ConditioningSetArea` 像素口径 / 分主体各自 Edit 再合成 / 2511 原生 ControlNet 几何）。
- 单测：`api/src/test/java/studio/weaveora/job/JobLayoutRegionsTest.java`（8 例：三档优先级、兜底点名、非法框丢弃、无锚定不动正词）。

### 4.1 提示词「点名主体」规则（P5，用户硬要求）

LLM 侧（提示词模板 + 后端注入）与后端兜底**双管齐下**：

- `api/src/main/resources/prompts/director_video_system.md`：新增「点名主体（P5，硬规则）」——必须用**方案主体名**点名、写明 left/right/center + foreground/background，**禁** `a man` / `the woman` 泛称；多主体逐个点名并写相互关系。
- `prompts/director_image_system.md`、`prompts/director_rewrite_shot.md`：同规则。
- `DirectorService` 生成用户消息时注入：主体清单 + **`image1=宝玉、image2=可卿`** 映射 + 点名硬规则；`rewritePrompt`（AI 同步提示词）两条分支都加「保留并点名原正词里的角色专有名词」。
- 后端兜底：见上表（`applyLayoutRegions` 拼 `Reference image mapping: ...`）。

## 5. 最近完成 & 待办

**已完成**
- 2511/2512 下载+校验（字节/头/sha256）+ 加载测试；工作流与参数切新模型；位置路线A 生效。
- `AI 同步提示词` **中文/英文弹框**：前端 + 后端 `lang` 支持 **均已上线**（jar 已补部署，2026-09-16 10:40）。
- ✅ **待办 1：API jar 补部署** —— 已部署；同时补上 `InternalController /progress` 回传 `cancelRequested`（上一提交只改了 JobService 与 worker，取消信号本来传不到 worker）。
- ✅ **待办 2：逐镜位置编辑器** —— 新组件 `web/src/components/director/ShotLayoutEditor.vue`（拖拽 + 右下角 w/h 缩放 + ±1% 微调 + 自动均分 + 继承方案区域 + 清除），挂在分镜卡展开区的「画面位置（本镜）」，写 `shots[].layout`；后端新增方案级区域回退（修掉「走剧情主体路径时 refs.regions() 恒为 null，方案区域根本到不了 worker」的旧缺口）。
- ✅ **待办 5：提示词点名主体** —— LLM 模板三处 + `DirectorService` 用户消息注入（含 `image1=主体名` 映射）+ `rewritePrompt` 硬规则 + **后端兜底映射**（`Reference image mapping: image1 = 宝玉 (upper-left, …)`）；新增 8 例单测。
- 另：`DirectorService.SYSTEM_FALLBACK`（读不到 prompts/*.md 时才用的内置兜底）同步补上「点名主体」规则，避免兜底路径漏规则。

**待办（优先级）**
1. ~~补部署 API jar~~ ✅ 已部署（2026-09-16 10:40，含 `cancelRequested`）。
2. ~~UI 位置编辑器写 `shots[].layout`~~ ✅ 已完成（web 已部署）。
3. **路线 B 离线对照**（640×352，3~4 组）—— ⏸ 用户指示暂停。**新增结论：这条路在 Edit 档基本是死的** —— 研究查到 `ConditioningSetAreaPercentage` 包住带参考图的 conditioning 报 `IndexError` 是 **ComfyUI 核心限制**（`CONDList` 在 `process_cond()` 里完全忽略 `area`，而 `get_area_and_mult()` 仍会把 latent 裁到区域尺寸），不是我们接线错。要区域控制得换方案（见 §7.4）。
4. **3/4/6 镜关键帧重跑**—— 用户已手工重跑过；后续优化见 §7。
5. ~~提示词规范：点名主体~~ ✅ 已完成。

---

## 7. 关键帧出图：用户报的 6 个问题 + 优化调研（2026-09-16 下午）

### 7.1 逐条处置

| # | 用户报的问题 | 根因 | 处置 |
|---|---|---|---|
| 1 | LLM 更新时**运镜关键帧**（如第 3 镜 2 帧）的起始/结束帧没更新，还是英文 | 「AI 同步提示词」只重写 shot 级 `positive_prompt`；而生成时**每帧用的是自己那帧的 `positive_prompt`**（`JobService` 逐帧建 still 任务）→ 帧提示词永远停留在旧稿 | API `director/rewrite-prompt` 新增 `frames[]`，**整镜（含每帧）一次重写**；返回 `keyframes[]`，前端逐帧回写。LLM 提示词里要求「帧数与输入逐个对应、每帧正词匹配它自己的 composition、`positive_prompt` 仍填结束帧」 |
| 2 | 「AI 同步提示词（全部）」太大导致卡死 | 前端 `for` 循环逐镜串行调 LLM（几十次），无并发/无进度、期间浏览器像死了 | **删除全局按钮与批量弹框**；改为**每个分镜卡上一个「AI 更新提示词」**（一次一镜）。`aiSyncAll / aiBatch* / openAiLang / confirmAiLang / aiBatchApply` 全部移除 |
| 3 | 画面位置的 x/y/w/h 不能直接填值 | 只有 ±1% 步进按钮 | 每个值改成**可输入框**（填百分比 0–100，回车/失焦生效）；越界自动把左上角回推而不是裁掉尺寸 |
| 4 | 选中文时运镜也要中文 | 同 #1（帧根本没被重写） | 同 #1：语言在**整镜**上统一（shot 级 + 每帧），`lang=zh` 时帧正词也是中文；弹框里加了「中文 / English」单选，切换即重新生成 |
| 5 | 负向词一半中文一半英文 | 系统**追加**的英文段落：`enforcePortraits` 的锚定句、`negWithRefGuard`（防串脸负词）、`MOTION_NEGATIVE`（静止类，clip）、位置句 —— 用户选中文后这些仍是英文 | 新增 `JobService.isZhText()` 语言探测（汉字 ≥6 且占比 ≥20%；「英文里带中文角色名」不会误判）→ **锚定句 / 位置句 / 防串脸负词 / 动效负词全部有中文版本**（`MOTION_NEGATIVE_ZH`）；LLM 侧再加一条「语言必须整条一致，禁止混入英文单词」硬规则 |
| 6 | 第 3 镜第 2 帧跑关键帧「没参考图」；没指明主体应提示/弹框让用户勾选 | 镜文本（action/zh/positive_prompt）没点到任何主体时后端**回退为「全部主体」**——多角色必然串脸；某些帧看起来就像没拿参考图。根因是**提示词里没点名主体** | 新增 `shots[].cast`（本镜主体名单）：① 镜卡新增「本镜主体」勾选 + **红色告警**（`shot-cast-warn`）+「标记为空镜」；② 生成/出片前**弹框**列出所有「没点名主体」的镜让用户勾选（`preflightCast`，可在弹框里标空镜）；③ 后端 `castOf()` 优先于文本匹配，`cast=[]` = 空镜（只留无主体风格图，不注入任何人物） |

> **#6 的真凶（2026-09-16 追加排查）**：第 4 镜的 `referenceSubjects` 是 **["宝玉","可卿","警幻"] 三个主体**，
> 而 Edit 工作流只有 **2 个 LoadImage 槽** → `_wf_set_image` 把第 3 张（警幻）**静默丢掉**，
> 正词却还写着 `image3 = 警幻` —— 于是这一镜里**戏最多的警幻反而没有任何参考图**，模型只能自己编脸。
> 这与「别称」无关（生产方案里 subjects 已是 宝玉/可卿/警幻 + 各自 aliases + 定妆照，没有别名重条目），
> 但**别称归并**仍然照修（前端按别称标注会新建一个没定妆照的主体 → 同样丢锚定）。两者都做了。

> 说明：#6 的「没参考图」在**任务 payload** 里查不出证据 —— 生产库 4 小时内的 still 任务 `referenceKeys` 全是 2 张（`["宝玉","可卿"]`）。所以真正的问题是「主体靠猜」而不是「真的没传图」；按用户要求做成**显式勾选 + 生成前弹框**。

### 7.2 参数推送的真实问题（用户问「参数推送是否存在问题」→ 有）

- **`services.image.cfg` 是个死字段**：引擎配置页/配置里一直有 `cfg: 2.5`，但 worker 的 `apply_services()` **只读** `engine/comfyUrl/workflow/img2imgWorkflow/editWorkflow/model/steps/denoise` —— **`cfg` 从来没被读过**。于是「改了 cfg 没效果」，实际生效的只有工作流 JSON 自带的 `cfg`。
  → 已修：worker 新增 `IMAGE_CFG`（env 回退 `WEAVEORA_IMAGE_CFG`），优先级 = 引擎配置页 > `payload.params.cfg` > 工作流 JSON；启动日志现在会打印 `edit=` 与 `cfg=`。
- **`editWorkflow` 在配置页没有输入框**（只能在 DB/API 里塞） → 已补输入框 + 优先级说明。
- 结果核对（线上实跑校验）：`IMAGE_CFG=4.0 IMAGE_STEPS=40 IMAGE_DENOISE=1.0`，工作流 `steps=40 cfg=4.0 shift=3.1`。

### 7.3 已应用的出图档位（依官方/社区调研）

| 项 | 原 | 现 | 依据 |
|---|---|---|---|
| `ModelSamplingAuraFlow.shift` | 3.0 | **3.1** | ComfyUI 官方 `image_qwen_image_edit_2511.json` 唯一参数差异（2509=3.0） |
| `steps` | 20 | **40** | Qwen 官方 40；ComfyUI 2511 模板默认 40（turbo 关）。≈2× 时间（20 步约 88s → 40 步约 176s/帧） |
| `cfg`（true_cfg_scale） | 2.5（**且没生效**） | **4.0** | Qwen 官方 4.0 / ComfyUI 2511 模板 4.0；**直接决定提示词遵从度** → 是「位置/点名不听」的正解旋钮 |
| 参考图接线 | 参考图 → ImageScale → VAEEncode → KSampler.latent_image **且** TextEncodeQwenImageEditPlus 接 `vae+image1/image2` | 不变（**已与官方一致**） | 官方模板三套都是「双线接线」，没有 `EmptySD3LatentImage`；负词节点官方也用 `TextEncodeQwenImageEditPlus`（prompt 留空）—— 我们本来就是，✅ |
| 提示词规则 | — | 新增构图口径 | 主体占画面 **30%–60%**、同镜 ≤2 主体、沿运动方向留白；**靠正向描述塑景，别靠负词排除**（Qwen-Image-Edit 对负词不敏感，官方负词就是一个空格） |

> ⚠️ 回退方式（都在配置页，不用改代码）：`生成引擎配置 → 文生图 → 步数/cfg`；工作流 JSON 备份在 VPS `/opt/weaveora/workflows/qwen_image_edit_api.json.bak.<ts>`。

### 7.3.1 第 4 镜复跑仍串脸 —— 真因（2026-09-16 下午实测）

**先排除误判：位置其实生效了。** 那次成功任务的 `payload.positive_prompt` 尾部原文：

```
参考图对应关系：image1 = 宝玉, image2 = 可卿, image3 = 警幻。…
主体位置（归一化画面坐标，原点在左上；y 越小越靠上，框越大越靠近镜头）：
  image1 = 宝玉（右上，x=0.73，y=0.16，框 0.19x0.76）; image2 = 可卿（上方，x=0.49，y=0.18，框 0.17x0.78）;
  image3 = 警幻（左上，x=0.00，y=0.00，框 0.33x1.00）。…
```

worker 日志同一次也确认 **3 张参考图全部进了图**：
`refs=f091356a….png, 775813a9….png, 784c40c0….jpg`（3 张、未摘槽）。

**真因：三个主体里只有一个绑了"真定妆照"。** 查该次任务所用确认稿的 `subjects[].portraitAssetId`：

| 主体 | 绑定为"定妆照"的资产 | kind | 快照 subject | 日期 |
|---|---|---|---|---|
| 宝玉 | `…b93bdc0010` | **reference（上传素材）** | (无) | 09-12 |
| 可卿 | `…b961be0012` | **reference（上传素材）** | (无) | 09-12 |
| 警幻 | `…9b826cb60005` | **portrait（生成定妆照）** | 警幻 | 09-14 |
| 袭人 | `…b95b926b000f` | reference（上传素材） | (无) | 09-12 |

即：进模型的 3 张图 = **2 张 09-12 上传的素材图 + 警幻的定妆照**。后端是**故意**尊重这种绑定的
（界面「把所选参考图设为定妆照」= 用户显式指定，不能因为 kind 不是 portrait 就丢掉），
但代价是**参考集风格/构图不统一**（定妆照是标准角色设定图，上传图往往是随手图）
→ 多角色同框时身份互相渗透，表现为「可卿的脸跑到警幻身上」「白衣警幻像没定妆」。

**顺带确认不是别称问题**：4 个主体的 portraitAssetId 互不相同（不存在俩主体共用一张脸）；
方案里也没有"别名重条目"（`PlanSubjects.mergeAliasDuplicates` 已兜住这种情况）。

**已做的改进（本次）**
1. **正词里「imageN = 主体名」只写一遍**：原来 `refAnchor`（参考图对应关系）与 `applyLayoutRegions`（主体位置）
   各写一遍、排序写法略有差别 —— 用户猜的"方位内容相互影响"就是指这个。现在合并成**唯一一句**
   （`refAnchor` 只保留身份约束），避免模型分不清谁对应哪张图。
2. **位置总控的主体表新增「实际锚定图」标注**：显示该主体实际用的是 `定妆照` 还是
   `素材图（非定妆照）⚠`（含资产 id 后缀与原因 tooltip）—— 这类问题以后在界面上直接看得见，
   不用再去查库。生成前也能据此判断风险。
3. 「位置总控」重构为三级（方案默认 / 第 N 镜 / 第 N 镜·帧 M，可搜索下拉）+「自动分配」，
   条目按**主体**（不再按参考图）；分镜卡保留「在顶部编辑位置 →」快捷入口。

**给用户的可执行建议（按性价比）**
1. **给宝玉/可卿也生成"定妆照"并重新绑定**（主体面板 →「生成图像」/「选图」）——
   三个主体的锚定图统一样式后，串脸概率会显著下降。这是当前最高杠杆的一步。
2. 若仍串：把 3 主体镜**拆成两镜**（≤2 主体/镜），或走「分主体 mask 逐次 Edit 再合成」。
3. 可选 A/B：提示词里的 `imageN` 是否该改成模型内部的官方口径 **Picture N**
   （`TextEncodeQwenImageEditPlus` 源码里就是 `"Picture {}: <|vision_start|>…"`）—— 需一次对照实验。

### 7.4 调研结论：还能怎么提升（候选，未实施）

按「收益/风险」排序，均已核对来源：

1. **白底定妆照把构图带偏** —— 官方/社区最可靠的解法不是提示词，而是 **mask + Inpaint Crop & Stitch**（`lquesada/ComfyUI-Inpaint-CropAndStitch`）：mask 外像素物理上不被改动，从根本上消除白底/证件照残留与整体漂移。代价：要新增节点 + 改工作流结构（属"控制模型级"改动，建议先离线 A/B）。
2. **2511 模板的 `FluxKontextMultiReferenceLatentMethod("index_timestep_zero")`** —— 官方 2511 模板在正/负 conditioning 后各挂一个（2509/Aug 没有）。官方 Note 说「用 Comfy-Org 官方权重可不必，用第三方重打包权重才需要」；我们的 `fp8mixed` 是 Comfy-Org 命名，故**未加**（节点在 GPU 机上确认存在，可随时 A/B）。
3. **`TextEncodeQwenImageEditPlus` 内部一定把参考图缩到 ~1MP**（VL 分支 384²、VAE 分支 1024²、宽高 round 到 8）→ 社区用「输入宽高取 **112 的倍数**（LCM(16,14)）」消除内部重缩放的 zoom 漂移。⚠️ 但本项目「输出画布 = `latent_image` 尺寸」，1280×704 → 改成 112 倍数就会变成 2:1 或 1.75（**画幅会错**），所以**没动**；若将来先把画幅改成 1568×896（1.75，≈16:9）再谈。
4. **多主体同框的社区成功率排序**：(B) 抠图摆位 + Fusion LoRA 一次统一光影 > (C) 分主体 mask + Crop&Stitch 逐次 Edit > **(A) 原生多图 + "image 1 在左 / image 2 在右" 提示词（= 我们现在这条）** > (D) 画笔色块 mask。区域条件/attention-couple 类方案在 Qwen 系**不可用**（Regional Prompter 只支持 SD1.5/SDXL），且 #3 已证明 `ConditioningSetAreaPercentage` 包参考图 conditioning 是核心限制。
5. **负向词在 Qwen-Image-Edit 上基本无效**（社区 CFG 网格实验；架构上没做 CFG 训练）→ 正向描述替代负词；这也解释了为什么"加了很多负词"没效果。
6. PuLID / InstantID / IPAdapter-FaceID **不适用于 Qwen**（是 SDXL/Flux 的 attention patch）→ 别往这个方向投入；要提脸相似度优先「多参考图 + 更高 cfg + FaceDetailer 收尾」。

## 6. 重要纪律（血泪）

- **重启任何 GPU 服务必须走 `bash deploy/gpu2_restart.sh`**（先查库，有 queued/running 就拦；`FORCE=1` 才强推）。绝不在有任务时重启 —— 曾把用户 21:49 的对口型任务撞死（502 Cannot connect to host 127.0.0.1:8001）。
- **按 PID 精确杀**（`pkill -f` 会自匹配杀掉自己的 ssh）；用 `[x]` 防自匹配。
- spark 式排障顺序：先看 `/queue`（是否僵尸 prompt）→ 再看 worker 日志 → 再看 ComfyUI 日志（**过滤 `non matching host` 噪声**）。
- 交付一律给**可点开链接**（`web/_share/`）。
