# 可行性分析 · 文生图落地 GPU#2（2026-09）

> 结论性文档（按仓库纪律：评审/战略结论只写 `可行性分析*.md`）。
> 决策输入（用户 2026-09-15）：**选档 T3（Qwen-Image）**；**需要 FLUX 版一致性**；**成文后直接执行**。
> 现状：图片引擎 = cloud（Replicate/方舟等）；GPU#2 是本项目唯一在线的 GPU 机（RTX 4090 **48G**，Ubuntu 24.04，`/opt/weaveora` 持久卷）。

---

## 0. 结论速览

| 项 | 决定 |
|---|---|
| 主力出图模型 | **Qwen-Image fp8（20.43 GB，Apache-2.0）** —— 中文提示词/画面文字最强档 |
| 文本编码器 / VAE | `qwen_2.5_vl_7b_fp8_scaled`（9.38 GB）+ `qwen_image_vae`（0.25 GB），与主力同源 |
| 加速 | **Qwen-Image-Lightning 8-step LoRA**（1.70 GB）→ 8 步出图，48G 上 1024² 约 **5–12 s/张** |
| **一致性（参考图/定妆照锚定）** | **Qwen-Image-Edit fp8（20.43 GB）** —— Qwen 官方改图/多图参考，**Apache-2.0**，与主力共用 text encoder + VAE；这是"T3 的一致性" |
| FLUX 版一致性 | ⚠️ **需先解决许可与磁盘**：FLUX 生态的一致性适配器（XLabs IP-Adapter / PuLID）**基于 FLUX.1-dev**（BFL 非商用）→ 与本项目既有纪律冲突；且落地 FLUX.1-schnell 还需 +22.7 GB。详见 §6（Phase 2） |
| 阶段 | **Phase 1 立刻执行**：Qwen-Image + Qwen-Image-Edit + Lightning（≈**52.2 GB**，清理 uv 缓存后可放下）；Phase 2（FLUX + FLUX 一致性）待磁盘扩容/许可确认 |

---

## 1. 现有资源与磁盘账（GPU#2 实测）

| 项 | 值 |
|---|---|
| GPU / 系统 | RTX 4090 **47.4 GiB** 可用；Python 3.12.13 / torch 2.7.1+cu126 / ComfyUI 0.34.0（`/opt/weaveora/ComfyUI`） |
| 磁盘 | `/dev/vda1` 196 G，已用 136 G → **余 51 G** |
| 已有模型（保留） | A14B 双专家 fp8 27 G（出片）、ACE-Step 10 G（配乐）、umt5 fp8 6.3 G、wan_2.1_vae 0.25 G、两个 Wan LoRA 1.26 G、LatentSync 6.6 G、talk/EchoMimic 19 G、CosyVoice + venv ~13 G |
| **可回收** | **`/root/.cache/uv` = 15 G**（talk venv 安装残留的 uv 包缓存，`uv cache clean` 即释放，**不下线任何服务**）；`_dl` 0.95 G（旧下载碎片）、`talk_work` 等零头 |
| 清理后可用 | **≈66 G** |

> 纪律：本机同时是生产出片/配音/对口型的机器 → **任何下载/安装都必须后台、不得抢占 8001 的显存**（出图与出片串行）。

---

## 2. 文件清单（体积均以 ModelScope API 实测 `206 + Content-Range` 为准）

### Phase 1 · 立即下载（Qwen-Image 全家 + 一致性 + 加速）

| # | 文件 | 体积 | 来源（ModelScope API 端点，本机实测可达） | 落盘 |
|---|---|---|---|---|
| 1 | `qwen_image_fp8_e4m3fn.safetensors` | **20.43 GB** | `Comfy-Org/Qwen-Image_ComfyUI` → `split_files/diffusion_models/` | `models/diffusion_models/` |
| 2 | `qwen_2.5_vl_7b_fp8_scaled.safetensors` | **9.38 GB** | 同上 → `split_files/text_encoders/` | `models/text_encoders/` |
| 3 | `qwen_image_vae.safetensors` | **0.25 GB** | 同上 → `split_files/vae/` | `models/vae/` |
| 4 | `Qwen-Image-Lightning-8steps-V1.0.safetensors` | **1.70 GB** | `lightx2v/Qwen-Image-Lightning` | `models/loras/` |
| 5 | `qwen_image_edit_fp8_e4m3fn.safetensors` | **20.43 GB** | `Comfy-Org/Qwen-Image-Edit_ComfyUI` → `split_files/diffusion_models/` | `models/diffusion_models/` |
| | **合计** | **≈52.2 GB** | | |

- 许可：Qwen-Image / Qwen-Image-Edit = **Apache-2.0** ✓（可商用）；Lightning LoRA 见其仓库声明（Apache-2.0）。
- 下载口径（遵守 `Weaveora.md §0.2` 铁律）：**10 路 Range 分片 + `.meta.json` 断点续传 + 后台静默**，脚本内写死**期望字节**；≥1 GiB 校验「字节 + safetensors 头结构」，<1 GiB 加做全量 sha256（官方值取到就写死）。
- 预期下载时间：ModelScope API 10 路实测 8–25 MiB/s（本机历史值）→ 52 GB ≈ **40–110 分钟**（后台）。

### Phase 2 · 待确认（FLUX 线）

| 文件 | 体积 | 许可/风险 |
|---|---|---|
| `flux1-schnell-fp8.safetensors` | 17.24 GB | FLUX.1-schnell = **Apache-2.0** ✓ |
| `ae.safetensors`（VAE） | 0.34 GB | Apache-2.0 ✓ |
| `t5xxl_fp8_e4m3fn.safetensors` + `clip_l.safetensors` | 4.89 + 0.25 GB | Apache-2.0 ✓ |
| FLUX 一致性适配器（XLabs IP-Adapter / PuLID-FLUX 等） | ~1.2–1.7 GB + CLIP-ViT/EVA-CLIP 1.7 GB | ⚠️ **多为 FLUX.1-dev 派生 → BFL 非商用许可**，与本项目「FLUX.1-dev 不用于生产」纪律冲突 |
| **Phase 2 合计** | **≈22.7–26 GB** | 需磁盘再扩 ≥30 GB（当前 66 G − 52.2 G ≈ 13.8 G，不够） |

---

## 3. 集成方案（与现有架构对齐，复用既有通道）

1. **ComfyUI 工作流**（GPU#2）：
   - `qwen_image_txt2img.json`：`UNETLoader(qwen_image_fp8)` + `CLIPLoader(qwen_2.5_vl_7b_fp8)` + `VAELoader(qwen_image_vae)` + `LoraLoader(Lightning 8 steps)` + `KSampler(steps=8, cfg=1.0, euler/simple)` + `SaveImage`；导成 **API 格式**放 `/opt/weaveora/`（与 LatentSync 工作流同一放法）。
   - `qwen_image_edit.json`：`TextEncodeQwenImageEdit`（参考图 + 指令）→ 定妆照/参考图一致性（改图路线）。
2. **worker**（VPS）：沿用现有 `comfy_client` 通道；出图 kind=`still`/`image` 已有实现，只需：
   - 新增「本地出图」分支：payload 带 `prompt/negative/steps/cfg/size/seed/referenceKeys(+subjects/regions)` → 注入上面两个工作流；
   - 与 lipsync/talk 相同的**串行与显存让位**（出图前 `/free` 卸掉 A14B/LatentSync 缓存）。
3. **API/EngineSettings**：`imageEngine=gpu` 时走本机；模型/参数在「模型库 + 参数说明」里选（Qwen-Image + Lightning LoRA + steps/cfg/尺寸）；**不静默回退**，未配就报错引导去设置（既有纪律）。
4. **前端**：无需新 Tab；出图产物仍是 `still`/资产库图片（现有 UI 直接可见）。
5. **配额/成本**：本地出图不花钱，但吃 GPU 时间 → 建议 `QuotaService` 增加"本地出图每日张数"计数（防误批量刷图），沿用 fail-open。

---

## 4. 验收（跑通即验收，先看效果）

1. 出图：第 5 镜（或任一镜）用 Qwen-Image + Lightning 8 步出 1024² 一张 → **肉眼比对 cloud 版**（主体/风格/构图）；
2. 一致性：用该镜定妆照/参考图走 `qwen_image_edit` → 检查同一角色跨镜是否稳定（人工看 + ArcFace 相似度）；
3. 性能：记录**首次（含加载）/后续**单张耗时、峰值显存（与 A14B 出片互斥时的让位是否正常）；
4. 通道：任务→资产→导出全链路无回归（现有 still 流程不受影响）。

---

## 5. 执行步骤（成文后直接做）

```bash
# 1) 腾空间（不下线任何服务）
/opt/miniconda3/bin/uv cache clean            # 释放 ~15G
# 2) 后台下载 Phase 1（10 路 Range + 续传 + 字节校验）
setsid nohup bash /opt/weaveora/dl_qwen.sh > /opt/weaveora/logs/dl_qwen.log 2>&1 < /dev/null &
# 3) 下完自检（字节 + safetensors 头 + LoRA/VAE sha256）
# 4) 装工作流（txt2img + edit）→ 出第一张图 → 给你看效果
```

---

## 6. 待决项（Phase 2 · FLUX 线）

1. **许可**：FLUX 生态的一致性适配器（XLabs `flux-ip-adapter`、`PuLID-FLUX`）基本都基于 **FLUX.1-dev** → 非商用；若坚持要 FLUX 线一致性，建议：
   - (a) 只用 **FLUX.1-schnell（Apache-2.0）** 出图，一致性改走 **Qwen-Image-Edit**（跨模型用参考图改图）；或
   - (b) 若确需 FLUX 一致性：仅限内部评测环境（不用于生产成片），并在 engine settings 里显式标注；
2. **磁盘**：Phase 2 需要再多 ~30 GB → 请平台把数据盘扩到 **≥200 GB**（或明确允许删除哪些旧权重）；
3. 附带核实项：Qwen-Image 的 IP-Adapter 类适配器（若有 Apache-2.0 版本）可替代 edit 路线；Z-Image-Turbo（6B，很省很快）在 ModelScope 上未找到正确仓库名，待核实后再评估。
