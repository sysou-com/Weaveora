# ===== weaveora-web 环境变量（复制为 .env.development.local 按需覆盖）=====
# API 基地址。默认留空 = 同源：dev 走 Vite 反代 /api → http://localhost:8080（见 vite.config.ts），
# 生产由 nginx 同源托管静态 + 反代 /api。直连后端时改为 http://localhost:8080。
VITE_API_BASE_URL=
